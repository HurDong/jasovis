# AI 개발 시작 지침

이 문서는 자비스 저장소의 공통 개발 지침이다. 특정 AI 모델·IDE·운영체제에 의존하지 않는다.
현재 사용자의 명시적인 요청을 우선하고, 요청 범위 안에서 구현·검증을 진행한다.

## 처음 시작할 때

1. `git status --short`와 `git log -5 --oneline`으로 실제 작업 트리와 최근 변경을 확인한다.
2. [DEVELOPMENT.md](DEVELOPMENT.md)에서 서비스 구조, 기능별 수정 위치와 현재 제약을 읽는다.
3. [README.md](README.md)에서 사용자가 보는 기능과 사용법을 확인한다.
4. [SPEC.md](SPEC.md)의 해당 기능 절과 실제 코드를 대조한다. 주입 대상·순서는 `manifest.json`이 기준이다.
5. 해당 테스트 문서를 읽고 변경에 맞는 검증을 수행한다.

문서의 날짜가 붙은 실측·테스트 기록은 당시의 증거다. 현재 브라우저에서도 동작한다고 단정하지 않는다.
문서와 코드가 다르면 차이를 확인하고 요청 범위에서 정정한다. 오래된 구현을 복원하는 근거로 삼지 않는다.

## 개발 원칙

- Chrome MV3 확장, 바닐라 JavaScript/CSS, 빌드 없는 구조다. 요청 없이 프레임워크·서버·번들러를 도입하지 않는다.
- 기존 미커밋 변경을 보존한다. 요청받지 않은 reset/checkout/clean, 커밋·푸시·병합·배포를 실행하지 않는다.
- 페이지 내부 Angular/React 상태 접근은 해당 MAIN 브리지에서 처리한다. 기능 UI는 정해진 이벤트/API를 이용한다.
- 편집기의 답변 쓰기는 `setAnswer` 또는 `applyGptAnswers`로 처리한다. 질문·지원서 메타데이터는 임의로 수정하지 않는다.
- 대시보드·독립 모달 등은 Shadow DOM을 사용한다. 기존 목록·채팅·GPT 응답에 붙는 UI는 기능 전용 클래스/속성으로 CSS 범위를 제한한다.
- 사이트 원본 DOM·이벤트·드래그·입력 중인 초안을 보존한다. SPA 이동, 프레임 교체, 중복 주입과 작업 취소를 고려한다.
- 사이트 구조를 못 읽은 상태를 빈 목록·0개·성공으로 표시하지 않는다. 부분 반영·실패·확인 불가를 구분한다.
- 별도 수집 서버·자동 GPT 호출은 없다. 권한을 우회하지 않으며 데이터 처리 변경 시 [PRIVACY.md](PRIVACY.md)를 함께 확인한다.
- 재현용 문항·채팅·화면에는 가상 데이터를 사용한다. 실제 자소서·대화·개인 식별 정보·인증 정보는 fixture나 공개 문서에 남기지 않는다.

## 유지할 사용자 흐름

- 목록은 작성 중을 주요 작업 영역으로 두고, 작성 중·제출 완료를 위아래 독립 스크롤로 구분한다. 현재 반응형·원본 드래그 동작을 유지한다.
- 색은 기존 사이트 브랜드와 토큰을 사용한다. 주황을 넓게 반복하거나 저대비 색면을 늘리지 말고, 상태·경계·간격으로 구분한다.
- 내 메시지에서 `대화에서 보기`를 누르면 모아보기를 닫고 원래 메시지로 이동한다. 이를 오류로 보고 목록을 유지하는 동작으로 바꾸지 않는다.
- GPT 연동은 평소 응답의 문항 정보와 코드 블록을 읽는다. GPT에 ID나 별도 출력 규약을 보내게 하지 않는다.
- 입력 대상은 열린 자소설 지원서 탭 중 사용자가 선택하며 ChatGPT **대화별로** 기억한다. 공고 링크 자동 분석·계정 전체 지원서 조회가 아니다.
- 연결 유지가 자동 동기화를 의미하지 않는다. 매번 선택한 응답에서 적용을 눌러야 하며, 연결 변경·해제만으로 답변을 쓰거나 지우지 않는다.
- 명확한 대응에는 반복 확인 모달을 추가하지 않는다. 대상/문항이 모호하면 선택을 받고, 저장 버튼을 자동 호출하지 않는다.

## 검증·완료

- 문서만 수정했다면 링크·경로·명세 일치와 `git diff --check`를 확인한다. 제품 테스트를 실행했다고 쓰지 않는다.
- 코드 변경은 관련 자동 테스트를 실행한다. UI/브리지 변경은 관련 fixture와 가능한 범위의 실제 브라우저 검증을 추가한다.
- 로컬 fixture 성공과 실사이트 성공을 구분한다. 실제 지원서 시험 입력·저장·제출, 채팅 전송 등은 현재 요청의 구체적인 허용 범위를 확인한다. 일반 개발 검증은 가상 데이터로 한다.
- 결과에는 변경된 동작, 검증한 범위, 미검증 사항, 필요한 확장/페이지 재로드 방법을 적는다.
- 기능/계약/UX가 바뀌면 README·SPEC·관련 테스트 문서를 갱신한다. 개발 진입점이나 중요한 제약이 바뀌면 DEVELOPMENT도 갱신한다.

## 다른 도구에서 읽기

`CLAUDE.md`는 이 파일을 `@AGENTS.md`로 불러오는 진입점이다. 공통 지침을 두 파일에 복제하지 않는다.
문서를 자동으로 읽지 않는 도구에는 “저장소의 AGENTS.md부터 읽고 작업을 시작해”라고 전달한다.
