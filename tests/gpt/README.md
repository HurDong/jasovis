# ChatGPT 응답 일괄 입력 검증

## 단어 공백·뒤 재진술 안내 — 2026-09-15, 분석기 v2

별도 `codex/gpt-spacing-guidance` 브랜치와 `C:\Users\ehd_a\Documents\Git\jasoseol-gpt-spacing-guidance` 워크트리에서 구현했다. 원본의 미커밋 변경은 가져오거나 덮어쓰지 않았다. 커밋·원본 반영·병합·푸시는 하지 않았다.

최초 가상 재현은 네 테스트 중 세 개가 실패했다: 한국어 경계 공백과 표식 없는 재진술 안내는 대응하지 못했고, 반대로 `※ 최근 3년…` 같은 새 조건의 생략은 자동 허용했다. v2는 이 두 방향을 함께 수정했다.

- `spacing-fixtures.cjs`, `spacing-guidance.test.cjs`: 본문·보기 동일/공백 차이, 안내만 생략, 두 차이의 조합, 마침표 없는 `주십시오` 종결, 경험 서술의 원인·방법·판단 근거·결과 목록, 부분·역순 응답을 검증한다. 원문·답변·UTF-16 구간 보존과 비교 근거 종류도 확인한다.
- 음성: 번호 충돌, 성공/실패·포함/제외, 기간·개수·대상·필수·선택 범위, 알 수 없는 안내, 양쪽 안내 충돌, 동일 본문 대상 여러 개, 숫자·영문·단위·소수 경계, 한 글자 어절/경계 이동, 깨진 괄호·안내 뒤 추가 요구·앞 안내·중복 응답은 수동이다. 긴 지침 호환 경로도 숫자·영문 공백 검사를 우회하지 않는다.
- 새 단어 공백 허용은 왼쪽의 한글 어간 2자 이상 + `의/을/를/에서/으로`, 오른쪽 한글 2자 이상의 경계이며 삽입과 삭제가 섞이면 보류한다. 모든 한국어 띄어쓰기나 자연어 의미를 판정하는 기능은 아니다. `본인의경험`은 처리하지만 알 수 없는 경계는 계속 추천/선택으로 남긴다.
- `background.test.cjs`: 준비 결과의 원문 안내·자동 대응 근거 전달과 자동 학습 0건을 추가했다. 기존 버전·연결·문항 지문 무효화, 수동 선택의 검증 후 저장, 준비 이후 변경과 되돌리기 검사는 유지한다. 분석기 v1 기억은 버전 필터에서 제거하며 v2로 이관하지 않는다.
- `spacing-guidance.browser.cjs`: 실제 MV3 worker/content script/MAIN 경로로 네 문항 자동 입력, 안내 펼쳐보기, 답변/질문 보존, 기억 0건, 390px 폭과 스크린샷, 되돌리기, 부분·역순 입력을 확인한다. 준비 후 원문 질문·기존 답변·GPT 응답 각각의 변경을 멈춤 지점에서 재현하고 입력 거부를 확인한다. 저장 호출·페이지 오류는 0건이다.
- 과거 `matching` fixture는 원문을 유지했다. 새 조건·선택 범위 안내를 생략한 사례는 자동 성공 기대값을 수동 선택으로 바꾸었다. `※`가 있으면 조건도 생략해도 된다는 과거 기록은 현재 계약이 아니다. 선택 후 입력·기억 재사용·삭제·되돌리기·복합 번호 이동 회귀는 계속 검증한다.

실행: 아래 Node 54개, 새 spacing fixture, 기존 matching·기본 browser·citation MV3 시나리오 통과. 긴 지침 경로를 바꾼 뒤 `JSL_GPT_DETAILED=1`의 browser 전체 시나리오도 통과했다. 출처 배지·코드 블록 추출 구현은 변경하지 않았다. 모든 입력·저장 관련 검증은 가상 페이지에서 수행했다.

```powershell
node --test tests/gpt/protocol.test.cjs tests/gpt/matching.test.cjs tests/gpt/engine.test.cjs tests/gpt/background.test.cjs tests/gpt/spacing-guidance.test.cjs
node tests/gpt/spacing-guidance.browser.cjs
node tests/gpt/matching.browser.cjs
node tests/gpt/browser.cjs
node tests/gpt/citation.browser.cjs
```

Playwright 패키지는 사용 가능한 런타임의 `NODE_PATH`에 지정한다. `JSL_GPT_SPACING_SHOT=<절대 png 경로>`로 새 fixture 적용 UI를 저장할 수 있다.

실사이트는 이미 열린 문제 대화의 질문과 대응 선택지 원문만 읽었다. 문제의 2·3·5번 질문을 로컬 분석기에 넣고 답변을 가상 문자열로 대체했을 때 세 번호 모두 대응, 미해결 0개를 확인했다. 그 질문·개인 답변은 fixture나 이 문서에 보관하지 않았다. 수정된 확장으로 실사이트 입력·저장·제출·GPT 전송은 수행하지 않았으며 모든 실제 자연어 표현의 정확성도 미검증이다.

최초 구현은 위 워크트리에만 있었다. 이후 사용자 요청으로 `C:\Users\ehd_a\Documents\Git\jasoseol`의 `main` 작업 폴더에 변경을 반영했다. 기존 미커밋 작업을 보존했고 커밋·푸시는 하지 않았다. 원본 경로에서 로드한 확장을 재로드한 다음 ChatGPT와 자소설 양쪽 페이지를 새로고침하면 사용할 수 있다. 원본에서 Node 54개를 다시 통과했다.

## 웹 출처 배지 추출 — 2026-09-15

`citation.browser.cjs`는 가상 사이트에 실제 MV3 확장을 로드한다. 질문 옆에 중첩된 `webpage-citation-pill` DOM과 사이트명·`+1`을 넣으면 기존 코드가 세 문항을 자동 대응하지 못하는 것을 재현했고, 배지를 추출에서 제외한 뒤 세 문항의 자동 대응·입력·되돌리기를 확인했다.

- 제목·질문 중간·질문 끝의 배지를 제외하고, 출처 UI와 원본 DOM·코드 복사 버튼·답변 원문은 보존한다.
- 같은 출처 이름이 일반 링크나 평문에 있으면 보존한다. 알 수 없는 표식과 배지 뒤 필수 조건도 삭제하지 않아 대응을 보류한다.
- 선택 화면 이후 실제 질문이 변경되면 기존 응답 지문 검증으로 입력을 거부한다. 저장 호출과 페이지 오류는 0건이다.
- 실사이트에서는 직전 원인 조사 때 출처 배지 DOM과 질문 원문의 일치를 읽기로 확인했다. 수정된 확장으로 실제 지원서 입력·저장·GPT 전송은 수행하지 않았다.

```powershell
node tests/gpt/citation.browser.cjs
```

## 공통 분석기·확인 대응 기억 — 2026-09-14

기준 `780b288`에서 관련 Node 검사 63개를 먼저 통과시켰다. 새 구현은 `engine.test.cjs`와 background 검사를 포함해 74개를 검증한다. 모두 가상 데이터이며 실사이트 읽기·입력·저장·GPT 전송을 이번 실행에서 수행하지 않았다.

같은 가상 질문을 Git의 기존 프로토콜과 새 프로토콜로 각각 비교한 결과:

| 변형 | 기존 | 새 구현 |
|---|---|---|
| 뒤의 중첩 언어별 분량 | 자동 | 자동 |
| 앞의 `[700자 이내]` | 추천 | 자동 |
| 다음 줄 `700자 이내 / 영문 1400자` | 선택 | 자동 |
| 질문 시작 `①` | 추천 | 자동 |
| 명시적 선택 허용 예시 | 선택 | 자동 |
| `갖게 된` → `갖지 않는` | 추천 | 선택, 추천 없음 |
| 본문 동일·번호 충돌 | 추천 | 선택, 추천 없음 |

`engine.test.cjs`는 두 가지 다른 문장 구조 × 여섯 번호 표기 × 세 분량 형태 × 세 배치(108개 조합), 구간 원문·반복 분석, 기호와 미분류 원문 보존, 필수/대상/기간/개수/부정 변경, 문항 순서 변경·추가·중복, 저장 표현의 유사 문장 일반화 금지, 상반된 이력과 추출 출처를 검사한다. 양성 조합은 모두 의도한 ID에 대응하며 음성·경계에서 잘못된 자동 대응이 없는지 단언한다. 이 유한한 결과가 모든 자연어의 의미 판별을 보장하지 않는다.

`background.test.cjs`는 수동 선택 후 검증된 결과만 보관, 자동/미확인 결과 제외, 연결·문항·버전 무효화, 대화 범위, 작업자 재시작, 256개/30일 정리, 삭제와 준비 중 학습 취소를 추가 검증한다. 입력 직전 선택하지 않은 문항까지 원문 구성 지문으로 확인한다.

`matching.browser.cjs`는 실제 MV3 메시지 경로에서 미선택 추천 → 수동 적용 → 표시 변형으로 기억 재사용 → 기억 삭제 → 다시 미선택을 확인한다. 앞쪽 분량과 별도 줄을 조합한 자동 적용, 원문 보존, 되돌리기, 정확한 입력칸 이동, 390px 폭과 저장 미호출도 확인한다. 기존 `browser.cjs`의 기본·번호 접두사·상세 지침 모드와 `feedback.browser.cjs`를 회귀 검증한다. 질문 바 전송은 가상 페이지에서만 실행한다.

```powershell
node --test tests/gpt/*.test.cjs tests/chat-tools/main.test.cjs
node tests/gpt/matching.browser.cjs
node tests/gpt/browser.cjs
node tests/gpt/feedback.browser.cjs
```

작업 경로는 `C:\Users\ehd_a\Documents\Git\jasoseol-gpt-question-engine`, 브랜치는 `codex/gpt-question-engine`이다. 해당 루트를 압축해제된 확장으로 로드한 뒤 ChatGPT와 자소설 양쪽 페이지를 새로고침해야 한다. 기존 설치 경로를 자동으로 변경하지 않았다. 최초 구현 단계에서는 커밋·푸시·main 병합을 하지 않았다.

## 본 질문과 언어별 분량 분리 — 2026-09-14

- 문항 번호·본 질문·대상 유일성 검사를 유지하면서 분량 안내를 내용으로 분리한다. 중첩/연속/전각 괄호와 대괄호, 한글·영문 분량, 생략된 표식 안내를 가상 네 문항으로 검증한다.
- `matching.test.cjs`에서 원문 보존, 번호 충돌, 중복 대상, 실제 요구가 섞인 분량 안내, 깨진 괄호와 추가 문장 보존을 검사한다.
- GPT·매칭·인용 질문·채팅 자동 검사 63개 통과. `matching.browser.cjs`에서 네 문항 자동 대응·입력·되돌리기 및 저장 미호출을 확인한다.
- 실제 대화는 읽기만 했으며 변경 후 실사이트 적용은 미검증이다. 확장과 양쪽 페이지를 리로드해야 새 로직이 적용된다.
- 이후 사용자가 “잘된다”로 이번 매핑의 실사이트 동작을 확인했다. 다른 지원서 유형이나 서버 저장까지 검증한 것은 아니다.

## 고른 곳 하나 GPT 질문 바 — 2026-09-15

기획: [docs/gpt-feedback-review-plan.md](../../docs/gpt-feedback-review-plan.md). 아래 “인용 질문 → 인용별 제안 받기”와 그 이전 절은 이 절이 대체한다.

- `feedback.test.cjs` (13): 고른 곳·질문 보관(이모지 범위), 빈 곳·공백·범위 밖·8,000자·4,000자 거부, 요청 형식(표지·문항·고칠 부분·주변 문맥·질문·기본 지시·규칙·답 형식,
  다른 문항·ID 미포함), 빈 질문, 주변 문맥 160자·문단 전체면 생략, 문항/지원서/원문 변경 거부, 활성 문항 읽기 실패,
  판독(정상·굵은 표기·다음 줄 진단/확인 필요, `ask`·`note`·`same`·`invalid`), 위치 확정·교체 거부, CRLF·앞뒤 공백, 뒤 탭 화면 갱신 보조(숨은 탭·표시일 때만 타이머, 취소 대응).
- `feedback-background.test.cjs` (23): 뒤 탭 그대로 전달·탭 미이동·새 대화 탭은 `active:false`, 뒤 탭 입력 실패 시 앞 탭 한 번 재시도 후 자소설 탭 복귀(실패해도 복귀),
  워커 재시작 뒤 중복 전달 없음, 승인 뒤 채널 실패는 확인 불가(칸 차지), 진행 중 기록 재전송 없음, blocked는 앞 질문 칸 보존, 새 질문이 앞 질문 기록 교체,
  승인 직전 원문·입력란·문서·문항·연결 재검증, 출처·프레임 거부, 동시 요청 직렬화, 중복 대화 탭 거부, 원본 탭 닫기 정리, 메시지 ID 미확인,
  스트리밍/완료 판독 보관·알림 한 번·완료 뒤 재판독 없음·불러오기 요약(답변 원문 미포함), 다른 지원서에서 불러오지 않음, 다른 출처·메시지·대화·형식 거부,
  바꾼 기록은 받은 수정안·고른 글과 같을 때만·보낸 탭만, GPT 탭 닫힘 표시·재감시, 정리·GPT 탭 앞으로, 확인 불가 이어받기.
- `feedback.browser.cjs` (12 단계): 임시 프로필에 실제 MV3 확장을 로드하고 자소설·ChatGPT를 가상 페이지로 대체한다. 가상 GPT는 `고칠 부분:`·`질문:`을 읽어
  형식 답을 두 번에 나눠 스트리밍하고 localStorage로 새로고침 뒤 대화를 복원한다.
  마우스·키보드(`Shift+→`) 고르기로 우클릭 없이 바 표시·고른 줄과 대시보드 미가림·밖 클릭 닫기 → `Alt+Q` 커서 이동·고른 곳 표시·칸 안 `Ctrl+S`/`Alt+2` 격리·
  대화 고르기·작성 중 GPT 입력으로 막힌 전송(글 유지) → 전송·요청 형식·쓰던 답변란으로 커서 복귀·밑줄 → 수정안 카드(글자 수·내 질문·진단·원문→수정안, 미가림)·
  `Esc` 접기·보기·바꾸기·검수 마커 층 갱신·되돌리기·카드 `Enter` 바꾸기·8초 뒤 닫힘·저장 0회 → 답만 온 질문·새로고침 복원·닫은 뒤 새로고침 미표시 →
  기다리는 사이 원문 편집(덮어쓰지 않음)·형식 오류 → 답하는 중 겹친 질문 차단·앞 질문 유지·GPT 탭 닫힘·다시 열기·이어받기·새 질문이 앞 질문 대체 →
  확인 불가 자동 재전송 없음·다시 보내기로 질문 칸 복원 → 늦게 뜬 메시지 이어받기 → 다른 문항에서 상태·가기 → CRLF·앞뒤 공백 → 넓은 화면 옆자리·`Esc` 질문 칸 닫기 → SPA 이탈을 확인한다.
  `JSL_FEEDBACK_SHOTS=<폴더>`로 단계별 화면을 저장할 수 있다.
- 헤드리스 가상 페이지는 모두 보이는 탭이라, 뒤 탭 전달·뒤 탭 화면 갱신 보조가 실제로 필요한지와 효과는 여기서 검증되지 않는다(워커 분기는 단위 테스트, 보조 스크립트는 가짜 창으로 확인).
- 미검증: 실제 ChatGPT 뒤 탭에서 ProseMirror 입력·전송 버튼 활성화, 뒤 탭에서 답 완료 판독(보조 스크립트 효과), 실제 사이트 레이아웃에서의 자리 찾기. 실사이트 시험은 사용자 허락 범위에서 한다.
- 사용하려면 확장을 다시 로드하고 자소설과 ChatGPT 양쪽 탭을 새로고침한다.

```powershell
node --test tests/gpt/feedback.test.cjs tests/gpt/feedback-background.test.cjs
node tests/gpt/feedback.browser.cjs
```

## 인용 질문 → 인용별 제안 받기 — 2026-09-14 (대체됨)

기획: [docs/gpt-feedback-review-plan.md](../../docs/gpt-feedback-review-plan.md). 아래 이전 절(질문창·공통 질문)은 이 절이 대체한다.

- `feedback.test.cjs` (13): 종류별 담기와 같은 범위 종류 변경, 겹친/이모지 범위, 요청 형식(표지·규칙·답 형식·종류별 요청·덧붙임, 다른 문항·ID 미포함),
  질문 종류 메모 필수, 편집 뒤 재배치(유지·유일 원문 이동·중복/삭제 시 위치 잃음), 문항/지원서/원문 변경 거부, 주변 문맥 120자,
  답 판독(정상·굵은/대괄호 제목·다음 줄 확인 필요·형식 오류·누락·중복·여러 코드블록·틀 문구), 받은 인용 길이 변화만큼 위치 보정·원문 불일치 시 교체 거부, v1 초안 거부.
- `feedback-background.test.cjs` (16): 기존 전송 안전성 10개에 더해 메시지 ID·인용만 보관하고 자소설 탭 재활성화, 메시지 ID 미확인 시 확인 불가,
  스트리밍/완료 판독 보관·알림·완료 뒤 재판독 없음·불러오기 요약(답변 원문 미포함), 다른 출처·메시지·대화·형식의 답 거부,
  GPT 탭 닫힘 표시와 재감시 해제, 보낸 탭만 정리·대화 탭 앞으로.
- `feedback.browser.cjs` (11 단계): 임시 프로필에 실제 MV3 확장을 로드하고 자소설·ChatGPT를 가상 페이지로 대체한다.
  가상 GPT는 요청의 `[인용 N · 종류]`를 읽어 형식 답을 두 번에 나눠 스트리밍하고, localStorage로 새로고침 뒤 대화를 복원한다.
  버블 3종·종류 칩·메모 단축키 격리·비활성 이유 문구·답변란 표시 → 편집 뒤 재배치·위치 잃은 인용 → 대화 고르기·작성 중 GPT 입력으로 막힌 전송 →
  형식 요청 전송·기다리는 중 → 답 도착·확인 필요·기존 적용 패널 미부착·GPT 답 아래 “자소설에서 받기”로 화면 다시 열기 → 받기·되돌리기·모두 받기(위치 보정)·수정됨 표시·저장 0회 →
  새로고침 복원 → 형식 오류 인용·기다리는 사이 원문 편집(덮어쓰지 않음) → GPT 탭 닫힘·다시 열기·감시 재개 →
  확인 불가 자동 재전송 없음·명시적 다시 보내기 → 확인 시간 뒤 늦게 뜬 메시지를 “보냈어요 · 답 가져오기”로 재전송 없이 이어받기 →
  다른 문항에서 답 도착 알림·이동 → 모델 CRLF·앞뒤 공백 차이 → SPA 이탈을 확인한다.
  가상 GPT는 실제처럼 보낸 메시지의 코드 블록을 꾸며 보여주므로, 전송 확인은 첫 줄 일치로 검증된다(2026-09-14 실사이트에서 원문 완전 일치 확인이 실패한 뒤 추가).
  `JSL_FEEDBACK_SHOTS=<폴더>`로 단계별 대시보드 화면을 저장할 수 있다.
- 회귀: `browser.cjs`, `matching.browser.cjs`, `tests/hotkeys/browser.cjs`, `tests/relay/browser.cjs`, `tests/list-cards/stats-browser.cjs`와
  GPT·매칭·엔진·채팅·릴레이·목록 단위 테스트가 통과했다.
- 2026-09-15 실사이트(사용자 요청 범위): 삼성증권 지원서 2번 문항 1곳 질문 전송 → 첫 줄로 전송 확인 → GPT 답 형식(`인용 1`/진단/수정안 코드 블록/확인 필요) 판독 → 받기로 답변 교체와 검수 마커 표시 갱신을 확인했다.
  단, GPT 탭이 뒤에 있는 동안에는 완료를 읽지 못했고 대화를 앞 탭에서 열자 바로 들어왔다. 그래서 보낸 뒤 GPT 탭에 머물고 “자소설에서 받기”로 돌아오게 바꿨다(이 변경의 실사이트 재확인은 아직).
- 미검증: 뒤 탭에서 답을 못 읽는 정확한 원인, 여러 인용·형식 오류 답의 실사이트 판독. 가상 편집기와 가상 응답은 실제 React/ProseMirror를 재현하지 않는다.
- 사용하려면 확장을 다시 로드하고 자소설과 ChatGPT 양쪽 탭을 새로고침한다.

```powershell
node --test tests/gpt/feedback.test.cjs tests/gpt/feedback-background.test.cjs
node tests/gpt/feedback.browser.cjs
```

## 현재 문항 인용 질문 — 2026-09-14 (이전 질문창 기준)

- `feedback.test.cjs`: 가상 원문으로 불연속/중복/겹친 선택, 같은 단어의 다른 위치, 고정 번호·삭제 참조,
  한글·이모지 범위, 주변 문맥 제한, 문항/지원서/원문 변경, 질문 없는 전송을 검증한다.
- `feedback-background.test.cjs`: 클릭 직전 원문·실제 입력란·문항·문서·연결 재검증, 발신 출처와 프레임,
  동일 요청/대화 동시 실행, 탭 중복, 원본 탭 닫기 정리, 워커를 새 VM으로 시작한 뒤 중복 전송 방지를 검증한다.
  클릭 승인 뒤 연결 실패는 확인 불가로, 클라이언트가 클릭하지 않았음을 확인한 경우는 중단으로 구분한다.
- `feedback.browser.cjs`: 임시 Chrome 프로필에 실제 MV3 확장을 로드하고 두 HTTPS 출처를 가상 페이지로 대체한다.
  선택 → 인용별/공통 질문 → 연결 → 웹 입력·버튼 전송 → 새 사용자 메시지 확인을 실행한다.
  문항별 초안 분리, 페이지 새로고침 복원, 번호 삭제 후 유지, 390px 창 폭, 기존 GPT 초안·첨부·생성 상태,
  입력 중 추가 편집, 원문 변경 뒤 재선택, 확인 불가 이후 재전송 방지, 닫힌 프로젝트 대화 재열기,
  질문창 내부 Ctrl+S/Alt+2 격리와 SPA 이탈을 포함한다. 자소설 답변 쓰기·저장은 0회여야 한다.
  확장에서 새로 만든 탭은 최초 탐색이 CDP 연결보다 빠를 수 있어 DNS를 차단하고, 확장이 요청한 URL을
  확인한 다음 같은 URL의 가상 페이지로 탐색한다. 실제 사이트의 로그인·첫 네트워크 탐색 성공을 검증하는 시험은 아니다.
- 실제 웹 GPT에서는 빈 `#prompt-textarea` contenteditable/ProseMirror와 form, 사용자 메시지 ID 구조를 읽기만 했다.
  실계정 질문 입력·전송이나 사용자 대화의 변경 후 종단 검증은 수행하지 않았다. 실제 전송 버튼과
  첨부 표시의 모든 변형을 확인한 것은 아니다. 가상 편집기는 실제 ChatGPT의 ProseMirror/React 실행 환경을 재현하지 않는다.
- 사용하려면 확장을 다시 로드하고 자소설과 ChatGPT 양쪽 탭을 새로고침한다.
- 검증 결과: 인용/전송 자동 검사 18개와 기존 GPT·매칭·채팅 회귀 44개 통과.
  현재 문항 질문, 기존 응답 적용·되돌리기, 복합/괄호 안내 매칭의 MV3 브라우저 시나리오도 통과했다.

```powershell
node --test tests/gpt/protocol.test.cjs tests/gpt/background.test.cjs tests/chat-tools/main.test.cjs
node --test tests/gpt/matching.test.cjs
node --test tests/gpt/feedback.test.cjs tests/gpt/feedback-background.test.cjs
node tests/gpt/browser.cjs
node tests/gpt/matching.browser.cjs
node tests/gpt/feedback.browser.cjs
# 질문 앞 번호가 붙은 지원서로 같은 MV3 시나리오 실행
$env:JSL_GPT_NUMBERED='1'
node tests/gpt/browser.cjs
Remove-Item Env:JSL_GPT_NUMBERED
# 단위 없는 분량·띄어쓰기·중첩 예시 생략 재현
$env:JSL_GPT_DETAILED='1'
node tests/gpt/browser.cjs
Remove-Item Env:JSL_GPT_DETAILED
```

브라우저 검증에는 Node에서 찾을 수 있는 `playwright`와 해당 Chromium이 필요합니다.
공용 패키지를 쓰면 `NODE_PATH`에 그 `node_modules` 경로를 지정합니다.

## 자동 검증

- `matching.test.cjs`: 가상 복합 문항 13개, 역순/부분 수정, 명시적 안내 생략, 종결 표현,
  잘못된/중복 문항명, 번호·질문 충돌, 본 질문/지침 변경, 유사도 추천·동률·중복·반대 요구와 원문 검증.
- `matching.browser.cjs`: 가상 복합 문항 13개의 실제 MV3 메시지·입력·되돌리기,
  `4-3` 부분 수정 후 10번 입력칸 이동, 추천 표시와 미선택 상태·미입력, 수동 선택 후 적용,
  390px 가로 초과 없음·페이지 오류 없음·저장 미호출.

- `protocol.test.cjs`: 자연스러운 전략·표·다섯 문항 응답, 일부/역순/수정안, 번호 없는 블록,
  Codex 지시문, 질문 표시 차이와 의미 차이, 양쪽 질문의 번호 접두사·번호 충돌·소수/연도/본문 숫자 보존,
  접두사 제거 후 중복 질문, 중복·충돌의 수동 대응, 원문 보존, 문항/답변 변경, 대화 ID 분리.
- `background.test.cjs`: 일회용 준비 토큰, 준비 이후 사용자 수정, 연결/응답/탭/문서 변경,
  되돌리기(입력 직전 복원, 일회용 토큰, 직접 고친 문항 유지, 연결·응답·문서·대상 변경 거부),
  중복 탭, 다른 응답·대화의 동시 입력과 처리 중 연결 변경, 발신 출처/프레임, 연결 메타데이터의 저장 범위.
- `browser.cjs`: 별도 임시 프로필에 실제 MV3 확장을 로드하고 두 HTTPS 출처를 로컬 fixture로 대체.
  자연 응답 → 최초 지원서 선택 → 준비 → 실제 service worker/content script 메시지 → Angular 모델 입력 → 재조회 흐름을 검사.
  언어 미지정/css/yaml/text, 중첩 pre, 제목·공백·줄바꿈 보존, 응답 폭 정렬과 390px 가로 초과 없음,
  부분/역순 수정, 재로드 후 연결, 충돌/중복 선택, 선택 중 응답 변경 거부, 스트리밍/재생성, 기존 복사 버튼,
  중복/부재 탭, 훅 실패·후처리 불일치, 문항 변경, SPA 대화 분리, 연결 변경/해제, 저장 미호출을 확인.
- 기존 채팅 도구 테스트도 함께 실행해 인접 기능의 회귀를 확인한다.

fixture 문항·답변은 가상 데이터다. 브라우저 테스트는 실제 자소설닷컴 지원서에 입력하거나 저장하지 않는다.
입력 결과는 fixture 모델 재조회이며 실사이트 서버 저장 검증을 뜻하지 않는다.

## 실제 사이트 읽기 확인 — 2026-09-10

열려 있는 ChatGPT 대화에서 첫 자연 응답의 다섯 문항, 문항 원문/글자 수/소재/전략 목록,
중첩 `pre` 안의 단일 `code`, `code` 텍스트의 줄바꿈, `section[data-testid^="conversation-turn-"]`,
완료 응답 복사 버튼, markdown 부모의 폭을 읽기 전용으로 확인했다.
실제 지원서 질문에는 `(200자)`, `(500자)` 같은 끝의 분량 표기가 있음을 확인했다.
이 구조를 가상 데이터 fixture에 반영했다. 실제 사용자 답변은 테스트 파일에 보관하지 않는다.

2026-09-10 검증 당시의 브라우저 도구에서는 `chrome://extensions` 탭을 조작할 수 없어 설치된 확장을 다시 로드하지 않았다.
따라서 그 검증에는 변경 후 실사이트 버튼 표시/위치, 실제 지원서 입력·저장 및 사이트 자체 자동 저장 확인이 포함되지 않았다.
확장을 다시 로드하고 두 사이트 페이지를 새로고침하면 새 기능을 사용할 수 있다.

### 질문 앞 번호 대응 검증 — 2026-09-11

- 자동 검사 31개 통과. 일치하는 번호 접두사, 번호 충돌, 본문 숫자 보존, 중복 질문과 원문 보존을 확인했다.
- 기본 지원서와 `JSL_GPT_NUMBERED=1` 지원서에서 MV3 브라우저 시나리오를 각각 통과했다.
  최초 연결·다섯 문항 입력·되돌리기·부분 수정·충돌 선택·재조회·저장 미호출을 가상 데이터로 검증했다.
- 이전 UI 문구 `연결: 예시기업 ICT 55`를 찾던 테스트를 현재 연결 제목 표시에 맞췄다.
- 변경 후 설치된 확장 재로드와 실사이트 재적용은 수행하지 않았다.

### 작성 지침 축약 대응 검증 — 2026-09-11

- 실사이트 읽기에서 단위 없는 최소/최대, `서술해 주세요`의 공백, 중첩 예시 생략 차이를 확인했다.
- 가상 문항으로 재현한 자동 검사 32개 통과. 본 질문/지침 변경, 번호 충돌, 중복 대상, 필수 조건 생략은 보류한다.
- 기본·번호 접두사·작성 지침 축약 세 모드에서 전체 MV3 브라우저 시나리오 통과.
- 사용자 원문 답변은 fixture에 보관하지 않았고 실사이트 입력·저장은 수행하지 않았다. 설치된 확장 재로드 후 실제 적용은 미검증이다.

## 남은 경계

### 복합 문항·안내 생략·추천 검증 — 2026-09-13

- 프로토콜·새 매칭·background·채팅 회귀 자동 검사 41개 통과.
- 기존 MV3 시나리오를 기본·정수 번호 접두사·상세 지침 축약 세 모드로 통과하고,
  별도 복합 문항/추천 MV3 시나리오도 통과했다. 재현용 데이터는 모두 가상 문항·답변이다.
- 실제 열린 대화의 문항명·추출 질문과 연결 선택지의 질문 13개를 읽고, 로컬 최신 비교기에
  답변을 가상 문자열로 대체해 대조했을 때 13개 대응·미해결 0개를 확인했다.
  개인 답변은 fixture에 저장하지 않았다. 이 결과는 현재 텍스트의 로컬 비교 검증이다.
- 설치된 확장 재로드, 변경 후 실사이트 적용·입력·저장 검증은 수행하지 않았다.
  새 코드 사용 시 확장 리로드 후 ChatGPT와 자소설 탭을 새로고침해야 한다.
- 2026-09-14 사용자가 “오케이 잘된다”로 변경 후 실제 사용의 정상 동작을 확인했다.
  모든 추천·충돌 예외 경로나 사이트 서버 저장까지 확인한 것으로 확대하지 않는다.

### 괄호 안내 생략 대응 — 2026-09-14

- 본 질문 뒤 `( ※ 안내 )`를 GPT가 생략한 경우, 기존 규칙은 여는 괄호 때문에 안내를 분리하지 못했다.
  가상 네 문항 재현에서 실패를 확인한 뒤 ASCII/전각·중첩 괄호의 균형과 안내 종료 위치를 검사하도록 보완했다.
- 자동 검사 44개 통과. 표식 없는 괄호·깨진 괄호·괄호 뒤 추가 요구·양쪽 지침 충돌은 보존/보류한다.
- `matching.browser.cjs`의 복합 문항·추천 회귀와 새 괄호 안내 네 문항 MV3 적용 시나리오 통과.
  질문/답변 원문 보존·선택 화면 없이 네 칸 대응·저장 미호출을 가상 데이터로 확인했다.
- 실제 대화의 질문/대상 문항 텍스트를 로컬 새 비교기로 대조해 4개 대응·미해결 0개를 확인했다.
  답변은 가상 문자열로 대체했으며 개인 답변은 fixture에 저장하지 않았다.
  설치된 확장 재로드와 변경 후 실사이트 입력·저장은 수행하지 않았다.

### 적용 문항으로 이동 — 2026-09-12

- 자동 검사 32개 통과. 실제 MV3 fixture에서 단일 2번 및 역순 다중 입력의 첫 문항으로 전환하고 표시 답변을 확인했다.
- 문항 전환 실패·질문 변경 시 오류 안내와 답변 보존을 검증한다. 탭/다른 창 활성화와 저장 미호출도 검사한다.
- 별도 `codex/gpt-review-question` 작업 트리에서 검증했다. 설치된 확장 변경과 실사이트 검증은 수행하지 않았다.
- 이후 사용자 요청으로 기존 작업 폴더에 이번 변경만 반영하고 자동 검사 32개와 MV3 브라우저 시나리오를 다시 통과했다. Chrome 확장 리로드 및 실사이트 적용은 미실행이다.
- 이후 사용자가 “오케이 잘된다”로 적용 후 해당 문항 이동의 실사이트 동작을 확인했다. 다른 예외 경로와 서버 저장 검증으로 확대하지 않는다.

사이트 DOM이 바뀌거나 문항과 코드 블록의 관계가 드러나지 않으면 버튼을 표시하지 않을 수 있다.
질문 문구/띄어쓰기가 실제로 다르거나 후보가 모호하면 선택 화면이 필요하다.
연결은 프로젝트가 아닌 대화 ID 기준이며, 대화 ID 없는 새/임시/공유 화면은 자동 연결하지 않는다.
준비 도중 작업자가 재시작되거나 90초가 지나면 다시 적용해야 한다.
저장 버튼을 호출하지 않지만 사이트의 기존 입력 훅이 수행하는 자동 저장까지 차단하는 기능은 아니다.

## 적용 후 탭 확인 — 2026-09-11

- 자동 검사 25개 및 기존 MV3 브라우저 시나리오 통과.
- 적용한 정확한 탭 선택, 다른 창 포커스, 입력·저장 추가 호출 없음, 390px 가로 넘침 없음을 확인했다.
- 모의 검사에서 탭 소실·다른 지원서 이동·연결/응답 변경 시 전환 거절을 확인했다.
- 2026-09-11 구현 후 사용자가 “오케이 잘된다”로 적용 후 확인 버튼의 실사이트 동작을 확인했다.
  다른 창 전환·탭 소실·연결 변경 등 개별 경계 조건은 위 로컬 검증 범위다. 서버 저장 검증은 아니다.
- 이후 코드를 수정하면 확장 리로드 후 ChatGPT와 자소설 탭을 새로고침한다.

## 사용자 확인 및 원본 통합 — 2026-09-14

사용자가 “잘된다 합쳐라”로 실제 사용의 정상 동작을 확인하고 원본 통합을 요청했다. 관련 Node 검사 74개를 다시 통과한 변경을 `main`에 반영한다. 원본 확장 로드 경로는 `C:\Users\ehd_a\Documents\Git\jasoseol`이며, 원본에서 로드한 확장을 다시 로드하고 ChatGPT·자소설 양쪽 페이지를 새로고침한다. 이 확인을 모든 자연어·예외 경로나 서버 저장 검증으로 확대하지 않는다. 푸시와 워크트리 삭제는 수행하지 않는다.

### 질문 바 공고 패널 배치 회귀

가상 왼쪽 공고 패널을 띄운 좁은 화면에서 질문 칸과 공고·선택 줄·대시보드의 겹침, 화면 안 배치, 실제 좌표의 가림 여부를 검사한다. 표시 층 검사는 다음 프레임의 DOM 생성을 기다린다. 실사이트 재확인은 별도다.

질문을 적은 상태에서 바깥 클릭·답변란 클릭·입력칸 밖 Esc·닫기 버튼으로 닫히고 질문 글이 보존되는지 가상 브라우저로 검사한다.

질문 칸 시각 구분 변경은 기존 브라우저 흐름과 스크린샷으로 확인한다. 인용 영역과 질문 입력 라벨·테두리 구분, 높이 증가 후 배치·닫기 회귀를 확인한다.

드래그 없이 문장 안에 커서만 둔 상태에서 Alt+Q로 검수 문장 전체를 질문 칸에 가져오며 GPT에 전송하지 않는 경로를 브라우저 fixture에서 확인한다.
