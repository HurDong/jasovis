# 자비스 (JASOVIS — 자소설닷컴 비서) — 공유 명세

서비스명: **자비스** ("자소설 비서"의 줄임말). 콘솔 로그 태그는 `[자비스]`.
스토어/공개 표기: **"자비스 for 자소설닷컴"** + 비공식(운영사와 무관) 고지 필수 — 상표 오인 방지.

Chrome MV3 익스텐션. 자소설닷컴 편집기·목록·채팅과 ChatGPT 응답 연동에 보조 기능을 얹는다.
**이 문서가 모듈 간 인터페이스 명세의 기준이다.** 처음 개발을 시작하면
[AGENTS.md](AGENTS.md)와 [DEVELOPMENT.md](DEVELOPMENT.md)를 먼저 읽는다.
날짜가 있는 실측은 당시의 기록이며, 뒤의 변경 기록이 앞의 설계를 대체할 수 있다.
현재 동작은 해당 기능의 최신 절, manifest와 실제 코드를 함께 확인한다.

## 검증된 사실 (2026-07-19, 실페이지에서 확인됨)

- 에디터는 **AngularJS 1.4.8**. React/Vue 아님.
- 문항 = `textarea.qna-question`(질문) + `textarea.answer`(답변) 쌍. 문항 수는 가변(1~N).
- Angular 스코프 체인을 올라가면 컨트롤러 스코프에 다음이 있음:
  - `resume`: `{id, user_id, title, created_at, updated_at, employment_id, end_time, submit_page,
    removed_at, category, category_updated_at, trashed_at, employment_company_id,
    employment_duty_group_ids, d_day, recruit_type}` (2026-07-22 실페이지 전체 키 확인됨).
    `employment_company_id`가 그 이력서와 연결된 채용공고(회사 단위) id — 아래 JD 패널 참조.
  - `qnas`: **배열이 아니라 객체** `{"1": qna, "2": qna, ...}`. 각 qna:
    `{id, number, question, answer, total_count, is_character, include_space, count_mode, active, is_autosave, ...}`
  - 컨트롤러 함수: `save`, `switch_qna`, `speller`, `restore_version` 등.
    `switch_qna`는 프로그래매틱 호출이 에러 없이 되지만 **화면 전환이 안 될 수 있음** → DOM 클릭 폴백 필수.
- 주요 DOM 셀렉터:
  - 문항 탭(왼쪽 1/2/3 버튼): `span.qna-number` (텍스트가 문항 번호). 클릭은 이 span 또는 부모에.
  - 맞춤법검사 버튼: `div.function_button.spell` — **`.click()` 호출로 검사 패널이 열리는 것 검증됨.**
    단, 이 버튼은 **토글**이라 패널이 열린 상태에서 누르면 닫힌다. 패널 열림 판정은
    재검사하기 버튼 `span.check-spell-button`의 존재+표시 여부(열려 있을 때만 보임).
    열려 있으면 재검사하기를 클릭할 것. (패널 헤더: `.side-tool-header.spell-header`)
  - 저장 버튼: 텍스트 `저장하기`.
  - 활성 문항의 답변란만 보임. 전체 문항 데이터는 DOM이 아닌 스코프에서 읽을 것.
- `navigator.clipboard.writeText`는 **유저 제스처(클릭/keydown 핸들러) 안에서만 성공.**
- 사이트에 자동저장/저장기록/맞춤법검사/메모 기능이 이미 내장돼 있음. 우리는 그 위의 보조 UI만.
- 사이트 전역 CSS 오염이 심하다. 독립 위젯·모달은 Shadow DOM을 사용한다.
  기존 목록·채팅·GPT 응답에 붙는 UI는 기능별 클래스/속성으로 CSS 범위를 제한한다.

## 아키텍처: 두 개의 월드

콘텐츠 스크립트 격리 월드에서는 페이지의 `angular`에 접근 불가. 그래서:

- `src/core/bridge-main.js` — `world: "MAIN"`으로 주입. 편집기 Angular 스코프를 읽고 액션을 실행.
  격리 월드와 `CustomEvent`로 통신한다. 목록·채팅의 페이지 내부 접근은 각 전용 MAIN 브리지가 맡는다.
- `src/core/bridge.js` — 격리 월드. 전역 네임스페이스 `window.JSL`을 정의.
  이 API를 사용하는 기능 모듈은 브리지를 통해 편집기에 접근한다. 기능 UI가 `angular`를 직접 만지지 않는다.
  채팅은 전용 이벤트 채널을, GPT 출처 간 연동은 runtime 메시지와 service worker를 추가로 사용한다.

manifest의 `content_scripts.js` 배열 순서 = 실행 순서: `bridge.js` → 각 feature 파일.
JSL 기반 기능 모듈은 로드 시 `JSL.register(name, initFn)`을 호출한다. 독립 채팅/GPT UI는 자체 초기화를 사용한다.

## JSL API (bridge.js가 제공, 기능 모듈이 소비)

```js
JSL.register(name, initFn)      // content script 로드 후 예약된 초기화에서 호출. 사이트 상태 준비는 별도 확인
JSL.getState() -> Promise<State | null>   // 스코프 스냅샷 요청 (단발)
JSL.onState(cb)                 // 클릭·입력 직후 State 브로드캐스트 구독. 2초 폴링은 안전망
JSL.action(name, payload) -> Promise<{ok, data?}>
   // 지원 액션: 'switchQna' {number}, 'spellCheck' {}, 'save' {},
   //            'setAnswer' {number, text} -> data {before} (아래 '붙여넣기' 절)
JSL.on(event, cb) / JSL.emit(event, payload)   // 격리 월드 내 기능 간 pub/sub
JSL.ui                          // dashboard 기능이 채워 넣는 UI 슬롯 API (아래 참조)
```

### State 스키마

```js
{
  resume: { id, title, end_time, d_day, updated_at },
  qnas: [   // 배열로 변환되어 옴, number 오름차순
    { id, number, question, answer, total_count,
      is_character, include_space, count_mode, active }
  ]
}
```

State가 `null`이면 스코프를 못 찾은 것(페이지 로딩 중이거나 사이트 구조 변경).
**모든 기능은 state null을 조용히 견뎌야 한다** (에러 던지지 말 것).

편집 페이지 상태는 문항 탭 `click`과 답변란 `input` 직후 다음 렌더 프레임에 전송한다.
80ms 뒤 비동기 후처리를 한 번 더 확인하며, 2초 폴링은 이벤트 누락 안전망으로만 둔다.
직전 상태와 `JSON.stringify` 결과가 같으면 전송하지 않아 불필요한 패널 재렌더를 막는다.

### 기능 간 pub/sub 이벤트 (JSL.on / JSL.emit)

- `'copy:qna'` payload `{number}` — 해당 문항 평문 복사 실행 (copy 기능이 구독)
- `'copy:full'` — 전체 프롬프트 모드 복사 실행 (copy 기능이 구독)
- `'paste:qna'` payload `{number}` — 클립보드를 해당 문항 답변에 붙여넣기 (paste 기능이 구독).
  `number`가 없으면 지금 연 문항.
- `'focus:answer'` payload `{number}` — 해당 문항 답변란에 포커스 (checkpoint가 구독).
  **문항을 전환하는 쪽은 항상 이 이벤트도 같이 쏜다** (hotkeys의 Alt+숫자, qna-nav 버튼,
  dashboard 카드 클릭) — 문항만 바뀌고 커서가 밖에 남으면 결국 답변란을 마우스로 눌러야 해서
  키보드로 옮긴 의미가 없다. 캐럿은 그 문항에 저장된 검수 위치로, 없으면 글 맨 끝으로 간다.
  전환이 비동기라 checkpoint 쪽에서 `number`가 활성화될 때까지 30ms 간격으로 최대 3초 기다린다.
  **성공 판정은 `focus()`를 부른 것이 아니라 `document.activeElement`가 그 textarea가 된 것이다**
  (2026-09-11) — 전환 직후 Angular가 다시 그리면서 방금 준 포커스를 걷어가는 경우가 있어,
  호출만으로 성공 처리하면 폴링이 멈추고 캐럿이 밖에 남는다.
  **검수 오버레이 부착 실패가 캐럿 이동을 막지 않는다** — 예전에는 `currentTa === ta`를
  통과 조건으로 걸었는데 `ensureAttached`가 내부 예외를 삼키므로, 부착이 한 번 어긋나면
  캐럿이 영영 안 들어갔다. 하이라이트가 붙는지와 커서가 본문에 들어가는지는 별개다.
- `'toast'` payload `{message, sub, kind, items, title, duration}` — 알림 표시 (dashboard가 구독/렌더)
  - **위치: 화면 우하단 고정**(`right:20px / bottom:20px`, 위로 쌓임). 위젯 shadow가 아니라
    body에 붙는 별도 호스트(`#jsl-toasts`)에 그린다 — 위젯 호스트에 `transform`이 걸려 있어서
    그 안의 `position:fixed`는 뷰포트가 아니라 위젯 박스 기준으로 잡히기 때문. URL이 자소서
    상세를 벗어나면 위젯과 함께 숨긴다.
  - `kind`: `'info'`(크림+주황 테두리) / `'ok'`(주황 채움+흰 글자) / `'fail'`(흰 바탕+빨강) / `'help'`
    — 색만으로 구분하지 않고 아이콘(i / 체크 / 엑스)이 함께 바뀐다.
    **초록은 쓰지 않는다** — 강조는 색을 추가하는 게 아니라 주황의 농도/반전으로만 만든다.
  - `sub`: 본문 아래 한 단계 작은 보조 줄 (예: `'616자 (공백 제외)'`).
  - `actions`: `[{label, primary, onClick}]` — 결정이 필요한 알림(붙여넣기 되돌리기 등).
    주면 **자동소멸·남은시간 바가 없어지고** 버튼을 눌러야 닫힌다. 같은 알림 병합도 안 한다.
    개수 제한(3개)에 밀려 사라지지도 않는다 — 밀려나면 덮어쓴 글을 복구할 길이 없어지기 때문.
  - `prev`: 본문 아래 회색 상자 한 줄 (덮어쓴 원문 미리보기 등).
  - `id`: 같은 id의 기존 토스트를 걷어내고 새로 띄운다. `JSL.emit('toast:close', {id})`로
    띄운 쪽에서 직접 닫을 수도 있다.
  - `kind:'help'`: `items`를 키캡 목록으로 렌더. `{keys, desc}`는 한 행, `{group}`은 구분 헤더 한 줄.
    자동소멸 없이 클릭·Esc·재호출로 닫힌다 (Esc는 capture에서 전파를 끊어 checkpoint의 Esc와 겹치지 않게).
    `keys` 표기에서 `+`는 동시 누르기, **공백을 두른 ` / `** 는 대안 키로 갈라져 각각 키캡이 된다
    (`'Tab / Shift+Tab'`). 공백 없는 슬래시는 키 이름 그대로다 — `'Alt+/'`는 `[Alt]+[/]`.
  - 같은 `kind|message|sub`가 연달아 오면 새로 쌓지 않고 기존 토스트를 bump + 타이머 리셋. 최대 3개.
  - `duration`(ms, 기본 3200) 동안 바닥의 얇은 바가 줄어들며 남은 시간을 보여준다.

### JSL.ui (dashboard가 구현하는 계약)

```js
JSL.ui.ready -> Promise<void>        // 대시보드 렌더 완료 시 resolve
JSL.ui.addAction(labelHTML, onClick, opts?) // 공용 버튼 추가, 버튼 엘리먼트 반환
//   opts {slot:'footer'|'header', variant:'primary'|'toggle'} — 생략 시 하단 기본 버튼.
//   'header'는 헤더 우측 소형 아이콘 버튼. 'toggle'은 on/off 표시를 호출측이 버튼에
//   .on 클래스를 토글해서 제어한다 (액션줄=라벨+스위치형, header 슬롯과 함께 쓰면
//   아이콘 색상만 바뀌는 소형 토글 — 둘 다 스타일은 dashboard 소유).
JSL.ui.addQnaAction(number, labelHTML, onClick, opts?) // 문항 카드 우측 액션 버튼
//   opts {icon, title, stages:['empty'|'active'|'done'], emphasis:'primary'}
//   icon 생략 시 복사 아이콘. stages 생략 시 모든 상태에 표시.
//   emphasis:'primary'는 작성중(확장) 카드에서만 주황 채움 버튼으로 승격되고 그 외엔 아이콘.
//   여러 개를 등록하면 등록 순서대로 한 묶음(.qna-btns)에 나란히 붙는다.
JSL.ui.setWarning(number, text|null) // 문항 행에 경고 문구 표시/해제 (예: "48자 초과")
```

dashboard가 없거나 5초 내 ready 안 되면, 다른 기능은 UI 추가를 포기하고
기능 자체(단축키/자동검사)는 계속 동작해야 한다.

### 답변 뱅크 문항 분류

분류 근거는 **문항 질문 텍스트만**이며, 답변 본문은 사용하지 않는다. 하나의 문항에
여러 의도가 있으면 복수 태그를 허용한다. 태그는 `지원동기·기업적합`,
`직무역량·전문성`, `입사 후 포부`, `가치관·조직적합`, `협업·조직문화`,
`성장과정·자기이해`, `강점·보완점`, `문제해결·도전`, `성취·실패`, `자유양식`,
`기타`다. 예를 들어 `입사 후 직무수행계획`은 지원동기가 아니라 `입사 후 포부`다.

편집 페이지의 답변 뱅크는 페이지 내부 플로팅 패널로 연다. 카테고리는 접이식 필터로
제공하고, `전체/현재 문항/즐겨찾기/최근 복사` 보기와 카드 펼치기를 지원한다. 현재 문항
보기는 활성 문항과 태그가 겹치는 다른 답변을 우선 노출한다. 패널을 열 때 활성 문항에
유효한 태그가 있으면 해당 태그를 상단에 표시하고 `현재 문항` 보기로 자동 진입한다.
즐겨찾기·최근 복사 시각은
답변 본문과 분리된 `jslAnswerBankMeta`에 저장한다. 패널 위치와 크기는 기억하며,
`Esc`로 닫고 `/`로 검색창에 포커스한다.

## 파일 변경 범위

초기 구현 때의 Agent A~E 분담과 코어 수정 금지는 현재의 파일 소유권 규칙이 아니다.
현재 요청에 필요한 파일을 수정하되 기존 변경과 모듈 간 계약을 보존한다.
동시에 작업 중인 개발자가 있다면 이번 작업의 변경 범위를 확인해 충돌을 피한다.
기능별 파일 지도는 [DEVELOPMENT.md](DEVELOPMENT.md)를 참고한다.

`src/features/qna-nav.js`는 파일만 남아 있고 manifest에서 로드하지 않는다.
문항 이동은 대시보드 카드 클릭과 `Alt+숫자` 단축키로 대체한다.

## 확정 키맵 (hotkeys 기능)

| 키 | 동작 | 비고 |
|---|---|---|
| `Alt+1`..`Alt+9` | 문항 전환(번호 지정) + 답변란에 포커스 | Ctrl+숫자는 크롬 탭 전환이라 가로채기 불가 |
| `Alt+↓` / `Alt+↑` | 다음/이전 문항으로 전환 + 답변란에 포커스 | 배열 순서 기준(qnas index), 처음·끝에서는 무시. 예전엔 checkpoint의 문장 이동 별칭이었으나 재배정됨 |
| `Alt+C` | 현재 활성 문항 평문 복사 | `JSL.emit('copy:qna', {number: 활성번호})` |
| `Alt+Shift+C` | 전체 프롬프트 복사 | `JSL.emit('copy:full')` |
| `F7` | 맞춤법검사 | `JSL.action('spellCheck')` |
| `Ctrl+S` | 저장 | `preventDefault()` 필수 (크롬 페이지저장 차단) |
| `Tab` / `Shift+Tab` | 검수 하이라이트를 다음/이전 문장으로 | **답변 textarea 안에서만** — checkpoint가 처리 |
| `Esc` | 답변란 포커스 해제 | Tab을 가로챈 대신 남긴 탈출구 (preventDefault 안 함) |

문장 이동 키는 hotkeys가 아니라 `checkpoint.js`가 답변 textarea에 직접 keydown을
붙여서 처리한다 (다른 입력칸의 Tab 포커스 이동은 그대로 둬야 하므로 document
capture로 잡지 않는다). 하이라이트는 캐럿이 속한 문장을 따라가므로, 이동은
`setSelectionRange`로 캐럿을 다음 문장 첫 글자에 놓는 것으로 끝난다. 처음·끝
문장에서는 아무 일도 하지 않는다.

keydown은 capture 단계(`addEventListener(..., true)`)로 document에 등록
(textarea 포커스 중에도 동작해야 함). IME 조합 중(`e.isComposing`)이면 무시.

## 글자수 규칙 (dashboard, copy, spellcheck 공용)

- 공백 포함 글자수: `answer.length`
- 공백 제외: `answer.replace(/\s/g, '').length`
- 바이트: `new Blob([answer]).size` (UTF-8)
- 제한: `qna.total_count` (0이거나 없으면 제한 없음으로 취급)
- `include_space`가 false면 제한 비교는 공백 제외 기준으로.

## 자소서 목록 페이지 (`/resume_list`) — 2026-07-19 실페이지 검증

편집 페이지와 별개의 AngularJS 화면(칸반 보드). 전용 모듈:
`src/core/list-main.js`(MAIN 월드) + `src/features/list-progress.js`, `list-stats.js`.

- 컨트롤러 스코프 진입점: `[ng-repeat="category_area in column.list"]` 엘리먼트에서
  프로토타입 체인을 올라가 `resumesInCurrentSeason`(현재 시즌 자소서 배열)을 찾는다.
- 각 resume: `{id, name, category, end_time, qnas[](답변 포함), ...}` — 목록인데도 qnas 전문이 포함됨.
- **카테고리 숫자 코드**: 0 작성중 / 1 제출완료 / 10 미제출 / 2,3 서류 합·불 /
  6,7 1차 합·불 / 8,9 2차 합·불 / 4,5 최종 합·불 / 100 AI마스터자소서.
  카드의 현재 카테고리 = 그 지원건이 도달한 가장 먼 단계 (뒤 단계 카드는 앞 단계를 통과한 것).
- 카드 DOM: `li.resume-node[resume_node_id="<id>"]`, 내부 `.name`, `.d-day`, `.date`.
  카드 이동은 드래그(`div.dropzone`). **카드 리스트는 수시로 재렌더되어 주입 노드가 사라진다**
  → 오버레이는 onState 주기 + MutationObserver로 재적용할 것.
- 목록 state 스키마: `{ page:'list', resumes:[{id, category, employmentId, employmentCompanyId, sample, qnaTotal, qnaFilled, endTime}] }`
  (`endTime` = `resume.end_time` 문자열 원본, 마감 임박 경고용. 없으면 null)
  (편집 페이지 state와 `page` 필드로 구분)

### 카드 우클릭 빠른 작업 (`list-menu.js`) — 2026-09-07 수정

- 자기소개서 쓰기 / 새 탭에서 쓰기는 유지한다.
- 채팅방으로 이동: 선택한 카드 ID를 `openListChat`으로 전달하고 MAIN에서 현재 시즌의
  `employment_company_id`를 다시 확인한다. 원본 `ChatCtrl`의 부모 `open_chat_window()`와
  `open_chat {employment_company_id}` 이벤트로 같은 화면 오른쪽 기업 채팅방을 연다.
  공고 연결이 없는 카드는 비활성화한다. 메뉴 열기만으로 조회하지 않으며 메시지를 보내지 않는다.
  응답 성공은 네이티브 열기 요청 전달을 뜻하고, 방 조회/로딩은 사이트가 처리한다.
- 공고 보기: 모집 직무 ID(employmentId)가 아닌 공고 본문 ID(employmentCompanyId)로
  기존 JD 패널과 같은 `/api/v1/employment_companies/:id?skip_read_log=true`를 조회한다.
  현재 페이지의 본문 전용 모달에 이미지·텍스트·표를 표시한다. 모집 직무 목록·광고·통계는 포함하지 않는다.
- 공고 모달은 내부 세로 스크롤, 닫기 버튼, Escape, 바깥 클릭을 지원한다.
  닫을 때 요청을 취소하며 낡은 응답은 버린다. 본문은 허용된 태그만 재구성하고
  스크립트·iframe·이벤트 속성·위험 URL을 제거한다.
- 복사 하위 메뉴는 공고 링크 / 자기소개서 내용 두 항목만 둔다.
  공고 링크는 사이트의 공유 형식 `https://link.jasoseol.com/recruit/<employmentCompanyId>`를 사용한다.
- 자기소개서 내용 복사 시에만 `getListResumeText {id}`를 요청한다.
  선택한 자소서의 제목과 문항·답변을 문항 순서대로 평문으로 복사하며 줄바꿈을 보존한다.
  다른 카드의 답변을 수집하지 않고 기존 문항/답변과 배열 순서를 수정하지 않는다.
- 일정·메모·첨부파일, 자소서 링크 복사, 카드 제목 복사 항목은 제거했다.
- 메뉴를 열 때는 서버 요청 없이 기존 목록 메모리만 읽는다. 공고 보기 클릭 시에만 본문을 조회한다.
- Shadow DOM, 화면 가장자리 보정, 방향키/Home/End/Enter/Escape, 바깥 클릭·스크롤 닫기를 유지한다.
  Shift+우클릭 또는 카드 밖 우클릭은 브라우저 기본 메뉴를 유지한다.

#### 검증 범위

- 2026-09-08 채팅 추가: MAIN 모의 검사에서 선택한 카드의 공고 ID 사용, 외부 전달 공고 ID 무시,
  패널 열기 후 이벤트 전달, 기존 digest 대응, 카드/공고/ChatCtrl 부재 거절, 자소서 데이터 미변경을 확인했다.
  로컬 Chrome에서는 메뉴 → 카드 ID 전달 → 메뉴 닫힘, 공고 없는 카드 비활성, 실패 안내를 확인했다.
  실제 계정의 채팅방 로딩 완료는 확장 재로드 후 확인이 필요하다.
- 로컬 Chrome에서 공고 본문 모달, 페이지 URL 유지, 내부 스크롤 영역, Escape 닫기를 확인했다.
- 복사 메뉴에 두 항목만 남고, 선택한 카드의 문항·답변 및 공고 본문 ID를 사용한 공유 링크를 확인했다.
- MAIN 상태/액션 검사: 공고 본문 ID, 문항 순서, 줄바꿈 보존, 존재하지 않는 카드 거절,
  기존 문항 배열 미변경. 평소 상태 브로드캐스트에는 답변 전문을 추가하지 않았다.
- 본문 모달의 HTML 검사에서 스크립트/iframe/이벤트 속성이 제거됐다.
- 로컬 검사는 공고 응답과 클립보드를 가상 데이터/기록으로 대체했다. 실제 지원 데이터는 변경하지 않았다.
- 실제 확장 적용과 실제 클립보드 쓰기 확인은 확장 및 목록 페이지 새로고침 후 필요하다.

### 통계 산정식 (list-stats)

통계는 툴바 구석의 한 줄 바가 아니라 **보드 바로 위의 여정 트랙**(`#jsl-track`)으로 표시한다 (2026-09-09).
보드와 같은 격자(`--jsl-cols`)에 정렬해 각 단계 수치가 그 아래 카드 더미 위에 선다.
사이트 노드는 옮기지 않고 `.scheduler` 앞에 형제로만 끼우며, 재렌더로 사라지면 1초 주기로 되꽂는다.

- 왼쪽 칸은 이번 시즌 전체 건수와 구성 막대(작성 중 · 제출 완료 · 결과 확인)다.
- 각 전형 칸은 반원 게이지 + 통과 분수 + 대기 건수다. 게이지 색은 단계 채도 램프를 따른다.
- 칸 사이 화살촉은 지나온 구간이면 그 단계 색, 결과가 아직 없으면 회색이다.
  앞 화살촉이 밝아지고 0.22초 뒤 뒤 화살촉이 따라오는 1.7초 루프이며,
  `prefers-reduced-motion: reduce`에서는 정지한다.
- 좁은 보드(`jsl-narrow`)에서는 트랙을 감춘다.

**대기 건수**는 새로 표시하는 값이다. 도달 = 앞 단계를 통과해 이 단계로 흘러온 수,
대기 = 도달 − 결과가 나온 수. 서류의 도달은 지원 건수다. 통과율 산정식 자체는 그대로다.


각 단계 분모는 "그 단계 결과가 나온 카드"만. afterX = 그 단계 뒤로 넘어간 모든 카드.
- 서류 통과율 = (2 + after서류) / (2 + 3 + after서류), after서류 = 6+7+8+9+4+5
- 1차 통과율 = (6 + after1차) / (6 + 7 + after1차), after1차 = 8+9+4+5
- 2차 통과율 = (8 + after2차) / (8 + 9 + after2차), after2차 = 4+5
- 최종 합격 = 4 / (4+5), 발표 대기 = 1, 지원 = 1+2+3+6+7+8+9+4+5
- 결과가 1건이라도 있으면 **언제나 %로 표기**한다. 결과가 0건이면 `–`.
  분모 5 미만일 때 분수로 바꾸던 규칙은 폐기했다 (2026-09-10) — 표본이 작다는 건
  바로 옆 캡션이 "N 통과 / M 결과"로 이미 말하고 있어 게이지까지 분수면 같은 말이 두 번
  나오고, 네 칸 중 한 칸만 표기 문법이 달라져 "저 칸만 계산이 안 됐나"로 읽혔다.
- 통과율 0%면 아크를 그리지 않는다. 길이 0에 `stroke-linecap="round"`를 주면 시작점에
  둥근 점이 찍혀 숫자 왼쪽에 주황 불릿이 붙은 것처럼 보인다.

### 진행률 표시 (list-progress) — 목록 A안, 2026-09-07

- 대상은 작성 중(category 0)이며, 문항이 0개이면 표시하지 않는다. 미제출(10)은 제외한다.
- 카드 하단 우측에 `작성 2 / 4` 같은 숫자와 44px 너비의 작은 주황 막대를 표시한다.
  큰 좌측 타일은 사용하지 않는다. 제목·마감과 겹치지 않도록 info/season 행에 106px 공간을 둔다.
- `.jsl-tile`과 `.jsl-seg` 이름 및 작성량 데이터 흐름은 유지한다. 한 칸은 한 문항이며,
  20문항을 넘으면 막대만 20칸 비율로 근사하고 숫자는 실제값을 유지한다.
- 미작성은 무채색, 작성한 숫자·막대는 주황색이다. 모든 문항을 채워도 제출 완료로 간주하지 않으며
  초록색이나 '완료' 문구로 표시하지 않는다. 작성 중에서 이동한 카드는 기존 unpaint 경로로 정리한다.
- 아래 갱신 경로는 유지한다. 과거 좌측 타일의 측정 관련 기록은 해당 날짜의 검증 이력이다.

#### 마감 임박 경고 (list-progress) — 2026-09-09

같은 모듈이 category 0 카드를 이미 순회하므로 경고도 여기서 붙인다. 별도 옵저버·조회를 만들지 않는다.

- 대상 = 작성 중 + 문항이 1개 이상 + 마감이 **오늘~내일 23:59(로컬)** 이내.
  **작성량과 무관하다** — 처음엔 미완성만 칠했으나, 다 쓴 임박 카드도 눈에 띄어야 한다는
  피드백으로 조건에서 뺐다(사용자 결정: "똑같이 칠하기"). 다 쓴 카드는 제출만 하면 되지만
  마감이 코앞이면 그 자체로 급하다. 마감이 이미 지났는데 작성 중인 초안도 경계보다 앞이라 포함된다.
- 마감 시각은 목록 state의 `endTime`(`list-main.js`가 `resume.end_time`을 문자열 그대로 전달)로 본다.
  판정은 격리 월드에서 하고 MAIN은 값만 넘긴다. `endTime`이 없거나 파싱 불가면 경고하지 않는다.
- 표시는 CSS가 맡는다. list-progress는 해당 `li`에 `data-jsl-urgent` 속성만 켜고 끈다.
  onState(2초) 주기에 매번 재평가하므로 시간이 지나 임박해지는 카드도 곧 반영된다.
- **칸(카드)만 칠한다** — 헤더 배지·상단 요약줄·카드 안 경고 줄은 두지 않는다(사용자 결정).
  옅은 주황 배경 `--jsl-tint` + 좌측 띠를 3→4px로 키움 + D-day 칩 주황 반전 + 진행 막대 트랙을
  임박 색으로. 사이트의 `.end-time.soon`(기준이 다름)과 무관하게 `data-jsl-urgent`로만 구동한다.
- 깜빡임 등 모션은 쓰지 않는다.

#### 갱신 경로 — 폴링 금지, 변화 시점에 당겨온다 (2026-07-26 실페이지 측정)

`JSL.onState`(2초 브로드캐스트)만 기다리면 드래그·새로고침 반응이 최대 2초 늦다.
실측: 유휴 상태에서 `JSL_STATE`가 **7초에 1회**만 도착(백그라운드 탭 타이머 스로틀).
그리기 자체는 병목이 아니다 — 카드 145장 순회 0.1ms, `getComputedStyle` 145회 0.2ms.

- MutationObserver 감지 → **60ms 디바운스 후 `JSL.getState()`로 직접 조회**. 왕복 0.2~0.6ms 실측.
  총 지연 약 60ms. `onState`는 안전망으로만 남긴다.
- 연속 변경(드래그 중) 대비: 진행 중 요청에 합류(`pulling`) + 요청 간 하한 250ms(`MIN_GAP`).
  변경 30회 연속 발생 시 조회 2회로 합쳐지는 것 확인.
- **`applyAll` 끝에서 `mo.takeRecords()`로 자기 유발 변경 기록을 버린다.** 안 버리면 주입이
  스스로를 다시 트리거한다.
- **디바운스에는 반드시 상한(`MAX_WAIT` 200ms)을 둔다.** 상한 없이 `clearTimeout` 후
  재설정만 하면, 사이트가 카드를 재생성하는 동안 변경이 끊임없이 들어와 **재도색이 변경이
  멎을 때까지 통째로 밀린다.** 2026-08-10 실측: 변경을 1.56초 흘리는 동안 재도색이 한 번도
  안 일어나다가 변경이 멈춘 지 2ms 만에 그려졌다. 그 사이 카드는 타일도 들여쓰기도 없는
  사이트 원래 모습이라, 사용자에게는 자비스 UI와 사이트 UI가 번갈아 나타나는 깜빡임으로 보인다.
- `visibilitychange` → visible 시 즉시 조회(백그라운드에서 낡은 상태로 복귀하는 것 방지).
- 초기 진입 시 첫 브로드캐스트를 기다리지 않고 즉시 조회, 스코프가 없으면 500ms 간격 재시도(최대 6초).
- `getComputedStyle`은 카드당 최초 페인트 1회만(`reserveSpace`). 읽기/쓰기를 섞으면
  카드 수만큼 강제 리플로우가 난다.

## 채용공고 JD 패널 (`src/features/jd-panel.js`) — 2026-07-22 실페이지 검증

편집 페이지 왼쪽 여백에 뜨는 위젯. 이력서와 연결된 채용공고 정보를 보여준다.
(자소설닷컴 편집 화면 우상단의 "채용공고" 버튼은 **이 공고와 무관하게 지원자 채팅방을 여는
버튼**이었다 — 실제 JD와는 다른 기능이니 헷갈리지 말 것.)

- **디자인 의도**: 카드/모달처럼 보이지 않게 배경·테두리·그림자가 있는 컨테이너를 두지 않는다.
  이미지/텍스트를 화면 왼쪽 위에 그냥 놓아 페이지 여백의 일부처럼 보이게 한다(사용자 요청:
  "뒷배경처럼"). 지원자수·조회수 등 부가 메타는 자리만 차지한다는 피드백으로 전부 뺐다 —
  이미지형이면 이미지만, 텍스트형이면 제목+본문만 보여준다.
- **접기 버튼은 접힘·펼침 모두 화면 왼쪽 끝(`left:0`)에 고정된 주황 세로 손잡이**
  (28×100px, 오른쪽만 둥근 모서리). 자리는 상태와 무관하게 안 움직이고 안의 내용만
  바뀐다 — 접힘은 `사진 아이콘 + "공고" + ›`, 펼침은 `‹ + "접기"`. 라벨은
  `writing-mode:vertical-rl` 세로쓰기. 펼친 이미지는 손잡이를 덮지 않도록
  `PANEL_LEFT`(=손잡이 폭+6)에서 시작한다.
  **위치 결정 이력** (같은 실수 반복 방지):
  - 처음엔 작은 흰 원형 버튼이었는데 접힌 상태에서 **버튼의 존재 자체를 못 알아채고
    "공고가 안 뜬다"고 오해**했다. → 접힘을 눈에 띄는 주황 손잡이로.
  - 펼침을 문항 탭 위 흰 알약("접기")으로 뒀더니, **알약이 탭보다 가로로 넓어서 오른쪽
    자소서 카드의 제목 줄을 파고들었다.** 탭 위 세로 여백은 있어도 가로가 탭 폭뿐이다.
  - 탭 **아래**로 옮기는 안도 검토했지만 그 자리는 사이트의 문항 추가/삭제 버튼
    (`add_qna()` / `removeMode()`, `li.add-qna` / `li.remove-qna`)이 이미 쓰고 있어 역시 겹친다.
  - 결론: 사이트 UI가 전혀 없는 **화면 왼쪽 끝**에 두 상태를 다 고정하면 겹침이
    구조적으로 불가능하다. 그래서 여기로 정했다.
  - 과거 버그: 탭 기준 `toggleTop`을 이미지의 `top`과 `Math.min`으로 클램프해서 버튼이
    이미지 모서리와 겹쳐 보였던 일이 있다. 지금은 탭 기준 계산 자체를 안 쓴다.
  아이콘은 유니코드 화살표(⌄, 폰트마다 굵기·크기가 들쭉날쭉) 대신 인라인 SVG.
  드래그 이동 기능은 없다 — 위치가 고정이라 필요성이 없음.
- **위치/크기는 고정값이 아니라 매번 실측**: 상단바 `.gnb`뿐 아니라 그 아래 두 번째 툴바
  `.function_bar`(맞춤법검사/저장기록/메모장/친구첨삭/자소서 전문 첨삭 받기 줄)까지 같이
  봐야 한다 — `.gnb`만 보고 좌표를 잡았다가 `.function_bar`를 가리는 걸 실페이지에서
  확인함. 왼쪽 문항 탭 `span.qna-number`와 `.resume-editor-wrapper`의 실제 `left`도 읽어서
  그 사이 빈 여백에만 정확히 들어가도록 top/left/width를 계산한다(`computeBounds()`).
  창 크기·줌 배율마다 여백 크기가 달라서 고정 px/vw 값으로는 툴바를 침범하거나 폭이 안
  맞았음. `resize` 이벤트 + 500ms 폴링(페이지 이탈 감지 루프에 합침)으로 재계산한다.
  (참고: 이 페이지 자체는 문서 스크롤이 없다 — `document.documentElement.scrollHeight`가
  `innerHeight`와 같음. 내부 콘텐츠만 개별 스크롤되는 구조라 패널을 `position:fixed`로 둬도
  "페이지와 같이 스크롤 안 됨" 문제가 생기지 않는다.)
- **"카드처럼 안 보이게" 튜닝**: 흰 배경/테두리/그림자/둥근모서리/hover 확대 다 뺐다. 이미지
  자체 스크롤(포스터가 화면보다 길 때)은 필요해서 남기되 스크롤바만 숨겼다
  (`scrollbar-width:none` + 웹킷 `::-webkit-scrollbar{display:none}`, 휠/드래그 스크롤은
  그대로 동작). 접기 버튼도 반투명 유리효과 대신 존재감을 최소화한 흰 원형 버튼으로.

- 연결고리: `resume.employment_company_id` (Angular 스코프, bridge-main.js가 snapshot에 포함).
- 조회: `GET /api/v1/employment_companies/:id?skip_read_log=true` — jasoseol.com 동일 출처라
  별도 권한/인증 불필요, 쿠키 자동 포함. 응답에 `title, end_time, view_count, resumes_count,
  employments[].field(모집부문명), content` 등이 있음.
- **`content`는 회사가 올린 원문 HTML 조각인데, 실페이지 확인 결과 상당수가 `<img>` 태그
  하나뿐인 "포스터 이미지형" 공고였다** (태그를 걷어내면 순수 텍스트 0자). 즉 담당업무·자격요건
  같은 JD 본문이 텍스트로 안 잡히는 공고가 흔하다 — DOM/OCR 없이는 그 이미지 안의 텍스트를
  가져올 방법이 없음. 그래서 `content`를 태그 제거한 순수 텍스트 길이로 텍스트형/이미지형을
  판별해서, 텍스트형이면 본문을, 이미지형이면 `<img src>`를 뽑아 크게 보여준다. 이미지 클릭 시
  이미지 파일 자체가 아니라 `employment_page_url`(회사 자체 채용 사이트, "채용 사이트" 버튼과
  동일 링크)로 새 탭 이동한다 — 이미지 URL로 바로 이동하면 브라우저가 다운로드로 처리해버림.
- fetch는 `employment_company_id`가 바뀔 때만 한다 (state는 타이핑마다 오므로 매번 재조회하면 안 됨).

## 답글 인용문 → 원본 메시지 (`chat-main.js`, `chat-react-main.js`, `chat-reply.js`) — 2026-09-08

- `https://jasoseol.com/*`에서 공통 Angular `ChatCtrl` 채팅 패널에 적용한다.
  `.message_target-message` 클릭 또는 Enter/Space로 원본을 찾고, 채팅 안에서 이동·강조한다.
  이동 바의 `답글로 돌아가기`는 처음 선택한 답글로 돌아온다. Escape/닫기로 안내를 없앤다.
- DOM의 `message_id`는 답글 자신의 ID다. 닉네임·문구 비교로 원본을 추정하지 않는다.
  클릭한 답글 ID 주변의 `/api/v2/chats/:chatId/messages`를 기존 Angular `$http`로 조회하고,
  응답에서 정확히 일치하는 답글의 `target_message.id`만 읽는다. 요청 제한시간은 8초다.
- 원본이 이미 렌더돼 있으면 채팅 컨테이너의 scrollTop만 바꾼다.
  미로딩 원본은 기존 사이트의 `open-chat-message {chatId,messageId,skipOpenChatroom:true}`로
  필요한 과거 대화를 불러온다. 원본 출현을 확인한 뒤 이동하며 12초 동안 없으면 실패 안내한다.
- MAIN 접근은 `src/core/chat-main.js`로 분리하고 `JSL_CHAT_REQ/RES`를 사용한다.
  기존 목록/편집기 응답 채널과 충돌하지 않는다. 선택한 답글/현재 방/허용된 원본 ID를 검사한다.
  원본 조회 응답이 오기 전에 방이나 선택이 바뀌면 폐기한다. 닫힌 패널에서는 요청을 거절한다.
- 삭제된 인용문은 조회하지 않고 안내한다. 조회 실패는 안내 후 재시도할 수 있다.
  텍스트 선택 중 클릭은 가로채지 않는다. 일반 말풍선의 신고·답장·좋아요 동작은 유지한다.
- 확장 서버/대화 저장/메시지 전송/상시 대화 수집은 없다. 해석한 ID 한 건만 메모리에 둔다.
  페이지 최초/새 메시지 삽입 시 인용문을 꾸미고, 이동 안내가 활성일 때만 DOM 위치를 확인한다.

UI 정리 (2026-09-08): Telegram의 인용문 → 원본 위치 이동을 주된 참고로 삼았다.
Slack의 별도 스레드 공간보다 좁은 기존 채팅 패널의 흐름에 맞는 방식으로 판단했다.
- 인용문은 회색 세로선·작은 이동 표시·12px 본문으로 표시하며 hover/선택에만 따뜻한 주황을 쓴다.
  받은/보낸 말풍선의 글자 대비를 각각 유지한다. 원본은 잠시 연한 주황 바탕과 왼쪽 선으로 강조한다.
- 헤더를 덮던 안내판을 제거했다. 대화 영역 우측 하단의 흰색 캡슐에 `답글로 돌아가기`만 표시한다.
  주황은 복귀 아이콘에 국한하고, 44px 버튼·닫기·키보드 포커스·reduced-motion을 지원한다.
- 입력란 위에 떠 있는 버튼으로 배치한다. 버튼 표시로 대화 컨테이너의 높이·padding·스크롤 길이를 늘리지 않는다.
  초기 구현의 임시 하단 여백은 불필요한 빈 공간이라는 피드백에 따라 제거했다.
- 조회 중에는 작은 진행 표시, 실패 시에는 짧은 안내와 재시도를 제공한다. 삭제 메시지에는 재시도를 두지 않는다.
- 참고: https://telegram.org/blog/reply-revolution?setln=en ,
  https://support.discord.com/hc/en-us/articles/360057382374-Replies-FAQ ,
  https://slack.com/help/articles/115000769927-Use-threads-to-organize-discussions

검증: 로컬 Chrome에서 실제 사이트에서 추출한 메시지 이동 함수와 모의 조회/렌더러를 연결해
로딩된 원본, 미로딩 과거 조회 후 이동, 돌아가기, 삭제/실패 안내를 확인했다.
MAIN 검사에서는 다른 원본 ID 거절, 다른 답글 응답 거절, 조회 중 방 전환/취소, 숨은 패널 거절을 확인했다.
UI 수정 후 로컬 CSS 400px 창에서 복귀/오류 캡슐의 가로 넘침 없음과 입력란 위 약 12px 간격을 확인했다.
복귀 시 캡슐·임시 여백 제거, 오류 재시도, Escape의 인용문 포커스 복원, 보낸 말풍선의 인용문 스타일도 확인했다.
실제 서버 응답을 이용한 확장 전체 동작은 아직 검증하지 않았다.
네이티브 과거 대화 로딩을 이미 시작한 뒤의 요청 취소는 사이트 함수가 담당하며, 확장이 중단시키지 않는다.

새 채팅창 지원 누락 수정 (2026-09-08): 실제 화면은 Angular 패널이 아니라 `/chat-slide` React
iframe이었다. 상위 문서에만 스크립트를 넣었던 초기 구현은 이 화면에 적용되지 않았다.
- `/chat-slide*`, `/desktop/chat-slide*`에 MAIN 어댑터와 공통 UI만 `all_frames`로 주입한다.
  편집기·목록 기능은 프레임에 추가하지 않는다. 독립 탭의 중복 UI 초기화도 방지한다.
- 인용문에 `원본 보기`를 항상 표시한다. React의 `TargetMessage` 인용문과 기존 Angular 인용문 모두 지원한다.
- 실제 DOM과 공개 클라이언트 코드에서 확인한 `#chatBody.chatBody-방ID`,
  `#chat-message-메시지ID`, 해당 행의 React props `children.props.message.target_message.id`를 사용한다.
  선택한 행의 JSX 자식만 제한적으로 확인한다. 구조 변경으로 정확한 ID를 얻지 못하면 추정하지 않는다.
- 이미 로딩된 원본은 즉시 이동한다. 미로딩 원본은 채팅 내부를 위로 이동해 사이트의 기존
  IntersectionObserver 로더를 작동시킨다. 사이트의 차단 사용자 필터·날짜 구분·메시지 캐시는 그대로 사용한다.
  45초, 진행 없음 8초, 최대 30회 목록 변화로 탐색을 제한한다. 찾지 못하면 답글 위치로 돌아가 안내한다.
- 다른 방 전환, 프레임 숨김, Escape/닫기 때 탐색을 중단한다. 이미 시작한 사이트 요청은 취소하지 않는다.
- 로컬 React DOM/props·IntersectionObserver 재현 검사: 표시된 버튼으로 현재/과거 원본 이동,
  답글 복귀, 삭제 안내, Escape 취소, 방 전환 취소, props 변경 시 실패 안내, 중복 초기화 방지를 확인했다.
  MAIN 단위 검사는 원본 ID 변조·방 불일치·취소 후 이동 거절과 Angular 응답 분리를 확인했다.
  기존 Angular 미로딩 원본 이동도 다시 확인했다. 실제 확장 재로드 후 React 서버 데이터 동작은 미검증이다.

## 내 메시지 모아보기·채팅방 빠른 이동 — 2026-09-08

- `chat-tools-main.js`와 `chat-tools.js`를 기존 Angular 페이지와 React 채팅 프레임에 주입한다.
  중복 초기화를 차단하며 기존 답글 브리지와 분리된 `JSL_CHAT_TOOLS_REQ/RES` 채널을 쓴다.
- 채팅 헤더 하단에 `내 메시지`·`빠른 이동` 버튼을 둔다. 흰 바탕·중립 회색 목록에 선택 상태만
  주황색으로 표시한다. 목록은 채팅 본문·입력란 영역을 덮고, 그 아래 원본 UI는 일시적으로 inert 처리한다.
  입력란을 제거하거나 값·사이트 작성 상태를 변경하지 않으며 닫을 때 이전 inert 값을 복원한다.
- 내 메시지는 현재 방에서 렌더된 행을 기준으로 한다. React는 해당 행의 제한된 JSX 자식에서
  `message`를 읽고 사이트가 계산한 `type === 'self'`만 선별한다. Angular는 원본 `.send` 구분을
  사용하고 인용문을 제외한 본문을 읽는다. 닉네임·색깔로 작성자를 추측하지 않는다.
- React의 날짜·시각을 표시하고, 날짜가 없는 Angular 행은 추정하지 않는다. 삭제 메시지는 본문 대신
  삭제 안내만 표시한다. 조회가 실패하거나 행 구조를 일부만 읽을 수 있으면 0개 성공으로 처리하지 않는다.
- 내용 검색은 메모리에 모인 본인의 메시지에만 적용한다. ID로 중복 제거하고 최신순으로 표시한다.
  항목을 누르면 원본 컨테이너 안에서 해당 행으로 이동·강조하고 키보드 포커스를 돌려준다.
- 내 메시지의 `이전 메시지 더 찾기`는 조회 기간 바로 아래, 목록 위에 고정한다.
  옅은 주황 배경·진한 주황 글씨와 테두리로 구분하고 탐색 진행·중단 안내도 같은 위치에 둔다.
  빠른 이동의 방 목록 새로고침은 기존 하단 배치를 유지한다.
- 조회 기간은 별도 제목 아래 13px의 진한 글씨로 표시하고 전체 기록 여부 안내와 분리한다.
- Alt+Tab·브라우저 탭 전환으로 문서가 비활성화되면 진행 중인 탐색만 중단한다.
  내 메시지·빠른 이동 패널, 검색어, 목록 스크롤은 유지하며 복귀 시 현재 방을 다시 확인한다.
  방 변경·페이지 이탈·사이트에서 실제 채팅 프레임을 닫은 경우의 정리는 유지한다.
- 이전 메시지 탐색은 사이트 스크롤 로더를 이용한다. 45초·최대 30회 진행·8초 무진행 한도를 적용하고,
  도중에 중단할 수 있다. 중단·일반 닫기는 기존 스크롤 앵커를 복원한다. 한도 도달은 전체 조회 완료가 아니다.
  방 전환·프레임 숨김·페이지 이탈·Escape에서 취소하며 이미 시작된 사이트 네트워크 요청은 취소하지 않는다.
- React 빠른 이동은 공개 클라이언트에서 확인한 채팅 저장소의 `myChats`와 `setRoomInfo`/`setPage`를
  사용한다. Webpack 런타임에서 저장소 메서드 조합이 유일하게 일치하는 모듈만 선택한다.
  모듈 번호 고정·모든 모듈 실행·메시지 캐시 덮어쓰기를 하지 않는다. 구조가 바뀌거나 일치가 모호하면 중단한다.
  Angular는 `ChatCtrl.chat_groups`의 `type: my` 목록과 `open_chatroom`을 사용한다.
- 방 목록은 ID·이름·읽지 않은 수만 UI에 전달한다. 전환 직전에 현재 방과 참여 목록을 다시 확인하며,
  목록 밖 ID·오래된 방 컨텍스트를 거절한다. 네이티브 메서드 호출 후 실제 방 ID 변경을 기다린다.
  검색창·이동 대상 메시지 상태는 사이트 전환 메서드로 정리한다. 새 방 생성·퇴장·메시지 전송 기능은 없다.
- 확장 전용 서버·추가 권한·영구 대화 저장은 없다. 도구 닫기·방 전환 때 UI 목록을 비운다.
  기존 원본 이동과 스크롤이 경합하지 않도록 `JSL_CHAT_TOOLS_OPEN`으로 원본 이동 안내를 닫는다.

검증: `node --test tests/chat-tools/main.test.cjs`로 본인 판별, 삭제 본문 제외, 일부 구조 변경,
숨은 패널·다른 방 거절, 참여 목록 재검증, 저장소 모듈 모호성, 중복 초기화를 검사한다.
`node tests/chat-tools/serve.cjs`의 가상 데이터 브라우저에서 React 내 메시지 7→20개(3회 과거 로딩),
검색·원래 위치 이동, 초안 유지, 삭제 반영, 중단·Escape 복귀, 빈 목록, 28개 방 검색·전환을 확인했다.
Angular 가상 화면에서는 본인 메시지 5개(인용 제외)와 참여 방 2개 조회·전환을 확인했다.
실제 확장 재로드 후 React 채팅 프레임에서 도구가 한 번만 주입되는 것을 확인했다.
참여 방 27개 조회·이름 검색·선택한 방의 헤더 변경·도구 목록 닫기까지 실제 전환을 확인했다.
현재 방의 본인 메시지 조회·내용 검색·원래 메시지 행 강조·입력란 복귀를 확인했고,
다른 방에서는 이전 대화 로딩 후 내 메시지가 0개에서 2개로 늘었다. 탐색 중단 안내도 확인했다.
실제 목록 화면과 함께 렌더된 채팅 도구의 배치·선택 색·긴 본문 줄바꿈을 확인했다.
이는 현재 배포된 React 채팅과의 검증이며, 전체 과거 기록 조회나 향후 사이트 변경 호환을 보장하지 않는다.

## 목록과 맞춘 채팅 테마 (`src/features/chat-design.css`) — 2026-09-08

- 모든 자소설닷컴 페이지의 Angular 채팅과 React `/chat-slide`, `/desktop/chat-slide` 프레임에 공통 CSS를 로드한다. 새 메시지·채팅방 교체는 CSS 선택자로 적용하며 별도 폴링이나 메시지 데이터 조회를 추가하지 않는다.
- B안 확정: 대화 배경은 흰색, 상대 말풍선은 `#ECEEF1` 회색·테두리/그림자 없음·짙은 글자, 내 말풍선은 브랜드 주황 `#FF6813`·흰 글자다. 말풍선은 12px 모서리와 발신 방향의 작은 모서리를 사용한다. React의 기존 `.flex-row-reverse`와 Angular `.send`로 발신자를 구분하며 이름/본문으로 추정하지 않는다.
- 내 답장 속 인용문만 `rgba(96,32,0,.18)` 배경과 흰 세로선으로 구분한다. 상대 인용문은 회색 말풍선보다 약간 어두운 배경·주황 세로선을 사용한다. 인용문은 원본 보기 라벨과 본문 미리보기 두 줄로 제한하되 DOM 원문과 접근성 라벨은 유지한다. 링크는 발신자 글자색을 따르고 밑줄을 유지한다.
- 원본 이동과 답글 복귀 강조는 발신자 배경/글자색을 유지한다. 내 원본은 흰 간격을 둔 짙은 주황 외곽선, 상대 원본은 주황 안쪽 선으로 강조한다.
- 헤더·입력창은 흰색과 목록의 회색 테두리를 사용한다. 공고 버튼 줄은 옅은 주황 배경 `#FFF5ED`와 따뜻한 경계선으로 구분하고, 호버 시 `#FFEBDC`로 강조한다. 브랜드 주황은 헤더 띠와 공고 아이콘에도 사용한다. 기존 닉네임 색, 메시지 정렬, 입력창 크기, 답장/검색/좋아요 및 이전 메시지 로딩 동작은 유지한다.
- 선택된 브랜드 주황 위 흰 글자는 작은 글자 WCAG AA 대비 기준을 충족하지 않는다. 사용자 확인 시안의 색을 유지한 선택이며 접근성 기준 통과로 간주하지 않는다.

### "내 채팅" 목록이 잘리던 것 — 사이트 버그 우회 (2026-09-10)

증상: 채팅 패널의 "내 채팅 31"에서 방이 18개까지만 보이고 스크롤이 없어 뒤쪽에 닿을 수 없다.

실측(`jasoseol.com/chat-slide` iframe 내부) — `MyChatItem` **31개가 전부 DOM에 있고**
목록 전체 높이는 **2153px**인데, 바깥 래퍼가 `relative w-screen h-screen overflow-hidden`
(사이트 자체 Tailwind 클래스)이라 iframe 높이 **1209px**에서 잘린다. 그 안에
**스크롤 컨테이너가 하나도 없다**(`overflow: auto|scroll`인 조상 0개). 892px이 잘려
**31개 중 18개만 닿았다** — 사용자 스크린샷의 행 수와 정확히 일치한다.

**우리 CSS 탓이 아님을 확인했다.** iframe의 `document.styleSheets`는 둘뿐이고(Toastify +
사이트 Next.js 번들) `chat-design.css`는 목록에 없다. 그 시트를 꺼도 `overflow: hidden`이
그대로다. 문제의 선언은 사이트 번들의 `.overflow-hidden { overflow: hidden; }`이다.
높이를 화면에 고정해 놓고 안쪽 스크롤을 주지 않은 사이트 버그다.

우리가 이미 이 프레임에 CSS를 넣고 있으므로 한 줄로 우회한다 —
`div.h-screen.overflow-hidden:has(.my-chats-list) { overflow-y: auto }`.

- `.my-chats-list`는 사이트가 붙인 의미 있는 클래스라 "내 채팅" 화면에서만 존재한다.
  채팅 홈에서는 매칭되지 않음을 확인했고(`matches()` false), 홈의 자체 스크롤러
  (`h-[calc(100vh-52px)] overflow-y-scroll`, 523px)도 그대로다.
- 사이트가 고치면 이미 스크롤되는 컨테이너에 `overflow-y: auto`가 겹칠 뿐이라 무해해진다.
- 검증: 적용 후 892px 스크롤되고 마지막 방이 완전히 보인다(`lastItemFullyVisible: true`).

## 채팅방 → 그 회사 공고 모달 (`src/features/chat-jd.js`) — 2026-07-27 실계정 검증

오른쪽 채팅 패널에서 **다른 회사(B사) 채팅방**을 열면 헤더 아래에 공고 버튼 줄이 생기고,
누르면 그 회사의 공고 목록이 화면 가운데 모달로 뜬다. 자소서 쓰던 페이지를 안 벗어나는 게 목적.

- **채팅 UI가 두 개다.** 이걸 놓쳐서 "어떤 방에서는 버튼이 아예 안 뜬다"는 버그가 났었다.
  둘 다 `div.chat-ctrl` 안에 들어 있고, 상황에 따라 한쪽만 보인다.
  | | A. React 채팅 | B. 페이지 내 Angular 채팅 |
  |---|---|---|
  | 뿌리 | `iframe[src*="chat-slide"]`의 `contentDocument` | `.chat-container.chat-window` 엘리먼트 |
  | 언제 | 상단바 채팅 버튼으로 열 때 | **자소서 페이지에서 사이트가 스스로 "이 기업 방"을 띄울 때** |
  | 헤더 블록 | `.sticky` | `.chat-window-head` |
  | 헤더 줄 | `h-[52px]` div | `.chat-info-wrapper` (52px) |
  | 뒤로/닫기 | `aria-label`에 back | `.close-chat` |
  **회사명 앵커 `a[href^="/companies/:id"]`는 양쪽에 똑같이 있다** — 그래서 뿌리만 일반화하면
  나머지 로직은 공유된다. iframe은 같은 출처라 `contentDocument`로 그냥 읽힌다
  (`all_frames`도, 추가 권한도 불필요).
- **"보이는 창" 판정을 크기로만 하면 안 된다.** 사이트가 채팅을 감추는 방식이 둘 다 다르다:
  - A는 iframe을 `display:none` + 0x0으로 만든다.
  - B는 **크기를 그대로 둔 채 패널을 화면 오른쪽 밖으로 밀어낸다**
    (실측: 열림 `left=2200` / 닫힘 `left=2560`, `innerWidth=2560`. `.chat-ctrl`의 `open-chat` 클래스가 토글된다).
  그래서 크기 + **뷰포트 교차**를 같이 본다. `offsetParent`는 `position:fixed`에서 null이라 못 쓴다.
- **숨은 창도 마지막으로 보던 방의 회사 앵커를 그대로 들고 있다.** 그래서 `querySelector`로
  창 하나만 집으면 "숨은 창에 버튼을 꽂아놓고 정작 보이는 창엔 아무것도 안 뜨는" 상태가 된다
  (실제로 보고된 증상). 보이는 창을 **전부** 찾아 각각 버튼 줄을 관리해야 한다.
- **열린 채팅방의 회사 식별**: 방 헤더의 회사명이 앵커다. `a[href^="/companies/:id"]`의
  `:id`가 곧 `company_group_id`. 실측: `/companies/269` = 현대오토에버, `/companies/4155` = 유진투자선물.
  방 목록 화면이나 회사 방이 아닌 채팅(전체·직무별·인사담당자)에는 이 앵커가 **없다** →
  버튼 줄을 자동으로 숨기는 판정에 그대로 쓴다.
  (백업 경로: `GET /api/v1/chats`의 항목 중 `type_number === 2`인 것이 회사 방이고 그 `type_id`가
  company_group_id다. 포스코 `type_id:14` ↔ `company_groups/14` 일치 확인. 지금은 앵커만 쓴다.)
- **회사 → 공고 전체**: `GET /api/v1/company_groups/:id/employment_companies`
  마감된 공고까지 전부 온다(현대오토에버 20건, 진행 중 0건). 각 항목에 `employments[]`가
  들어 있어 모집부문 조회를 위한 추가 요청이 필요 없다.
- **"내가 쓴 공고" 매칭**: `GET /api/v1/resumes`(내 자소서 전체, 270건).
  **연결 키는 `employment_company_id`가 아니라 `resume.employment_id`다** — 자소서는 공고가 아니라
  그 안의 모집부문에 매여 있다. `employments[].id` → 공고 id 역인덱스를 만들어 맞춘다.
  `/api/v1/resumes` 응답에는 `employment_company_id`가 아예 없으니 이 경로 말고는 방법이 없다.
  실측: 현대오토에버 5건, 앰코테크놀로지코리아 2건 정확히 매칭됨.
- **공고 본문**: `GET /api/v1/employment_companies/:id?skip_read_log=true` — JD 패널이 쓰는 그 API.
  텍스트형/포스터 이미지형 판별 규칙도 jd-panel과 동일(태그 걷어낸 순수 텍스트 15자 미만이면 이미지형).
- 통계 칩은 SPEC의 카테고리 코드를 그대로 쓴다. "서류 통과"는 `category === 2`뿐 아니라
  그 뒤 단계로 넘어간 카드(6,7,8,9,4,5)를 모두 포함한다 — 위 통계 산정식과 같은 정의.

### 구현상 주의

- **React가 우리 노드를 수시로 날린다.** 창마다 MutationObserver(120ms 디바운스)를 따로 걸어
  다시 꽂고, `mo.takeRecords()`로 자기 유발 변경을 버린다(list-progress와 같은 패턴).
  창의 등장·교체·가시성 변화는 옵저버로 안 잡히므로 500ms 폴링 + `resize`로 잡는다.
  헤더 줄 엘리먼트는 리렌더마다 새 노드로 갈리므로 **삽입 기준점은 매번 새로 찾는다**
  (캐시해 두면 엉뚱한 자리에 꽂힌다).
- **버튼 줄은 헤더 안이 아니라 헤더 줄 바로 아래**에 넣는다. 헤더는
  `flex ... justify-between` 한 줄에 회사명(≈239px)+아이콘 2개가 이미 335px를 다 쓰고 있어서
  안에 끼우면 라벨이 잘린다. 삽입 기준점은 `a.closest('.sticky')`의 직계 자식 중 앵커를 품은 줄.
- **모달은 iframe이 아니라 최상위 문서에** 그린다. iframe 안에 그리면 채팅 패널 폭(≈358px)에
  갇혀 포스터를 크게 볼 수 없다. `z-index: 2147483647`.
- **Esc는 두 문서 모두에 걸어야 한다.** 포커스가 채팅 iframe 안에 있으면 keydown이
  최상위 문서로 오지 않는다.
- 모달 패널은 `height` 고정이 아니라 `max-height`. 고정하면 공고가 3개뿐인 회사에서 아래가 텅 빈다.
- 조회는 전부 캐시한다: 내 자소서 1회, 회사별 공고 목록 1회, 공고 상세 1회.

## 붙여넣기 (`src/features/paste.js`) — 2026-08-04 실페이지 검증

ChatGPT 등에서 초안 **본문만** 복사해 온 뒤, 대시보드의 그 문항 카드에서 붙여넣기 아이콘
하나로 답변란에 넣는다. 목적은 "문항 탭을 옮기지 않는 것"이다 — 원래 불편의 원인이 그거였다.

- **DOM이 아니라 Angular 스코프에 쓴다** (`setAnswer` 액션, bridge-main.js).
  화면에 보이는 `textarea.answer`는 지금 연 문항 하나뿐이라 DOM 경로로 가면 문항마다
  탭 전환이 필요하다. ng-model이 `qna.answer`라 스코프에 쓰면 **비활성 문항도 탭 전환 없이**
  들어가고 그 문항 글자수 카운터까지 갱신된다(실측 확인: 3문항 중 비활성 3번에 쓰니
  `0 / 1000` → `19 / 1000`).
- **같이 불러야 하는 것 — 하나라도 빠지면 티가 난다:**
  - `qna_change(qna)` / `answer_keyup(qna)` — textarea의 `ng-change` / `ng-keyup`에 걸린
    사이트 자체 갱신 훅. 타이핑과 같은 뒤처리를 태워준다.
  - 보이는 textarea에 `input` 이벤트 디스패치 — **checkpoint.js가 textarea 글자색을
    투명(`color:transparent`)으로 만들고 자기 미러 레이어에 글자를 그리는데, 그 미러는
    `input` 이벤트로만 다시 그린다.** 스코프만 바꾸면 미러가 옛 글을 그린 채 남아
    붙여넣은 글이 **화면에서 안 보인다**(실측 확인). 비활성 문항에 썼을 때도 그냥 쏜다 —
    값이 그대로라 아무 일도 일어나지 않는다.
- **덮어쓰기 = 낙관적 적용 + 되돌리기.** 이미 내용이 있으면 일단 넣어서 눈으로 보게 하되
  원문을 들고 있다가, 되묻는 토스트에서 `적용`을 누르면 잊고 사이트 `저장하기`까지 대신
  누르고, `되돌리기`를 누르면 원문을 복구한다. 빈 문항이면 되돌릴 게 없으니 확인 없이 바로.
  - 결정을 미루는 사이 그 문항을 **직접 고쳤다면 되돌리지 않는다** (수정까지 날아가므로).
    마지막으로 써넣은 값(`after`)과 현재 답변을 비교해서 판정하고, 다르면 안내만 한다.
  - 같은 문항에 연속으로 붙여넣어도 `before`는 최초 원문을 유지한다.
  - 다른 자소서로 이동하면 대기 상태를 비우고 토스트를 닫는다.
- 클립보드 읽기는 `navigator.clipboard.readText()` — manifest `clipboardRead` 권한 필요,
  **유저 제스처(클릭) 안에서만** 성공. 실패는 예외 대신 안내 토스트.
- 붙여넣을 본문은 줄바꿈 표기만 통일(`\r\n`→`\n`)하고 앞뒤 공백만 턴다. 문단 사이 빈 줄은
  글쓴이 의도이므로 건드리지 않는다.

## 목록 정렬 어긋남 (`src/features/list-sort.js` + `list-main.js`) — 2026-08-10 실페이지 검증

증상: 공고를 한꺼번에 여러 개 담고 목록에 오면 필터는 `공고 마감일순`인데 카드는 넣은 순으로
늘어선다. 정렬 버튼을 다시 누르면 고쳐진다.

- **카드 순서는 클라이언트 정렬이 아니다.** 서버가 자소서마다 매겨준 순서값으로 그려진다
  (노드에 `total_order_point` / `duration_order_point` / `season_order_point` 세 개가 있다).
  `li.resume-node`에는 **`ng-repeat`이 없다** — 인라인 `top`과 `first-column`/`second-column`
  클래스로 직접 배치하는 커스텀 드래그 위젯이다. 그래서 스코프 배열을 정렬해도 화면은 안 바뀐다.
- 사이트 `sort(key)`가 하는 일 (함수 본문 확인):
  `POST /resume/scheduler_season_sort.json {season, sort}` → 응답의 `order_point`를 노드마다
  적용 → 재배치. 즉 **정렬은 서버 왕복이고 결과가 계정에 저장된다.**
- 그래서 자소서를 새로 만들면 그것만 낡은 순서값으로 끼어들어 어긋난다.
  **한 번 바로잡으면 다음에 새로 만들기 전까지는 안 깨진다** — 매 로드마다 고칠 일이 아니다.
- **같은 키로 눌러도 된다.** `sort()`에 "이미 그 키면 무시" 가드가 없어서 이미 활성인
  `공고 마감일순`을 눌러도 POST가 나간다(실제 클릭으로 확인). 생성일순을 갔다 올 필요 없다.
  단 **연속 호출은 경쟁 조건이 있다** — 비동기라 앞 요청 응답이 뒤 요청 결과를 덮어쓴다.

### 감사 규칙 — 오탐이 나면 멀쩡한 목록에 POST를 날리게 된다

- **`작성 중`(`ul.itemlist[category_key="0"]`) 섹션만 본다.** 섹션마다 방향이 다르다:
  마감이 남은 작성 중은 **임박 순(오름차순)**, 제출 완료(1)·서류 불합(3)처럼 이미 지난 건은
  **최근 마감 순(내림차순)**. 전 섹션을 오름차순으로 검사했다가 오탐을 냈다.
- 마감이 이미 지난 `작성 중` 카드는 방향을 확인하지 못해 검사에서 뺀다.
- `sortKey`가 `title`이면 방향을 확인하지 못했으므로 판정을 보류한다(`skipped`).
- 순서값을 직접 읽지 않고 **렌더된 DOM 순서**를 진실로 삼는다 — 세 order_point 중 어느 것이
  렌더를 지배하는지 동점 케이스에서 갈려 단정할 수 없었다.
- 접힌 섹션 판정은 `offsetParent`로 한다(`getBoundingClientRect`는 강제 리플로우를 부른다).
- 실측 비용: 카드 6장 기준 **1.2ms**, 네트워크 0회. 어긋나지 않았으면 DOM 쓰기도 0회.

### 액션 (list-main.js)

- `sortAudit` → `{sortKey, checked, needsFix, skipped}` (읽기 전용)
- `sortFix` → 사이트 정렬 버튼을 `menu.key === sortKey`로 찾아 `click()` (쓰기는 클릭 위임만)

`list-sort.js`는 `JSL.onState`에서 **카드 구성이 바뀐 순간에만** 감사한다. 교정은 페이지
로드당 최대 2회, 간격 30초 이상, 교정 후 1.8초 뒤 재감사해서 그래도 안 맞으면 **자동 교정을
끈다** — 우리 판정이 틀렸을 때 POST가 무한히 나가는 걸 구조적으로 막는다.

## 준비중 공고 담아두기 (`src/features/watch.js`) — 2026-08-10 실API 검증

자소서 문항이 아직 안 열린 공고("자기소개서 준비중")는 자소설닷컴에 자소서로 등록할 방법이
없다. 열람만 하고 나가면 잊혀 지원 자체를 놓친다. 그래서 **담아뒀다가 자기소개서 목록에서
계속 보이게** 한다. 토스트·데스크톱 알림은 쓰지 않는다(사용자 결정: 과함) — 목록에서
보여주는 것만.

- **판정 근거는 `employments[].has_resume` 하나뿐이다.**
  `GET /api/v1/employment_companies/:id?skip_read_log=true` 응답의 각 모집부문에 붙어 있다.
  전부 false = 화면의 "자기소개서 준비중", 하나라도 true = "자기소개서 작성".
  실측: 105406(서울특별시 통합채용) 49개 부문 전부 false / 105566(KT&G) 17개,
  105572(한국전력기술) 12개는 전부 true.
  **목록 API(`?start_date=..&end_date=..`)의 `employments`에는 `has_resume`가 아예 없다** —
  반드시 상세를 봐야 한다. 상세는 로그인 없이도 읽힌다.
- **모집부문 단위가 아니라 공고 단위로 담는다.** 한 공고에 자소서는 하나만 등록하므로
  부문별로 담을 이유가 없고, `has_resume`도 보통 공고 전체가 한꺼번에 열린다.
- 저장: `chrome.storage.local`의 `jslWatch` = `{ "<ecId>": {id, name, title, endTime, pageUrl, addedAt} }`.
  `chrome.storage.onChanged`로 다른 탭(공고 모달 ↔ 목록)이 서로 따라간다.

### 카드 3단계 — 목록을 열 때마다 다시 판정

| 상태 | 조건 | 카드 | 클릭 |
|---|---|---|---|
| `waiting` | 문항 미개시 | ⏳ 점선 주황 "문항 대기 중" | `/recruit?ec=<id>` (같은 탭) |
| `ready` | `has_resume` 하나라도 true | 주황 반전 "자소서 만들기 →" | `/recruit?ec=<id>` (같은 탭) |
| `stale` | 미개시 + 마감 D-2 이내 | 무채색 "회사 사이트에서 지원 →" | `employment_page_url` (새 탭) |

`stale`이 중요하다 — **자소설닷컴에 끝내 문항이 안 올라오고 회사 채용 사이트에서 직접
지원하는 공고가 있다**(서울시 통합채용 → `seoul.saramin.co.kr`). 열리길 기다리다 마감하는
게 제일 나쁜 결말이라 마감이 닥치면 카드가 알아서 외부 링크로 바뀐다. 초록은 쓰지 않는다.

**자동 정리**: `GET /api/v1/resumes`의 `resume.employment_id`가 그 공고의 `employments[].id`에
걸리면(=이미 자소서를 만들었으면) 목록에서 뺀다. 마감이 지나도 뺀다. 조회 실패는 **지우지
않는다** — 저장해 둔 값으로 그냥 보여준다.

### 주입 지점

- **공고 모달** (`/recruit?ec=<id>`, React/Next.js): 헤더 버튼줄(채용 사이트 / 채용 공고 공유 /
  공고 즐겨찾기) **바로 아래**에 배너 한 줄. Tailwind 클래스는 해시처럼 생겨 못 믿으므로
  `공고 즐겨찾기` 버튼 텍스트로 잡고 `closest('.grid')`로 올라가 `afterend`에 꽂는다.
  React가 노드를 수시로 날리므로 400ms 폴링으로 다시 꽂는다. `?ec=`가 없거나 모달이 닫히면 걷는다.
  준비중도 아니고 담아두지도 않은 공고에는 아무것도 안 띄운다.
- **자기소개서 목록** (`/resume_list`, AngularJS): `.list-scroll-area` **맨 앞**에 전용 섹션.
  - **보드가 두 개다.** 둘 다 `.scheduler-column.column2`인데 AI 마스터 자소서 쪽에만
    `.ai-resume-column`이 붙는다 — 반드시 걸러내야 '자기소개서 작성 지원' 보드를 잡는다.
  - 이 보드는 (옛 SPEC에 적힌 칸반이 아니라) `작성 중 (n)` / `제출 완료 (n)` / `미제출 (n)`
    **접이식 섹션 스택**이다. 각 섹션 = `.list-container` > `.header.pointer`(`.list-title` +
    `span.arrow`) + `.dropzone` > `ul.itemlist` > `li.resume-node`.
    그래서 자비스 섹션을 한 칸 더 얹는 게 사이트 문법 그대로다. 사이트 카드 사이에 섞어
    넣지 않는 이유: 머리글의 개수 "(4)"와 화면의 카드 수가 어긋나고, 사이트 카드처럼
    생겨서 드래그하려 들게 된다.
  - 카드 실측값(2026-08-10): 336×70, `padding 10px 6px 13px 53px`(53px = 사이트 7px +
    list-progress 타일 46px), bg `#fafafa`, border `1px #eee`, radius 4px.
    `.name` 13px `#333` / `.d-day` 11px 흰글자 bg `#999` radius 3px / `.date` 12px `#777`.
    섹션 머리글 `.list-title` 12px `#777`, `.list-container` margin-bottom 15px.
  - Angular 재렌더로 섹션이 날아가므로 MutationObserver + 폴링으로 다시 꽂는다
    (list-progress와 같은 패턴, `mo.takeRecords()`로 자기 유발 변경을 버린다).
    재주입 때는 마지막 계산값(`lastRows`)을 다시 써서 API를 다시 치지 않는다.

## 보드 높이 고정 (`src/features/list-layout.js`)

증상: 목록에 세로 스크롤이 **세 겹**으로 포개져 있다. 실측(2327×1194) —
바깥 `.resume-list-tmpl-container` 370px + 작성지원 칼럼 213px + 서류전형 칼럼 11,901px.
휠이 커서 위치에 따라 셋 중 아무거나 움직인다. 게다가 제출 완료 섹션이 화면 아래 끝보다
아래에서 시작해(2560×1313에서 y=1315) 스크롤 전엔 옮길 카드가 한 장도 안 보인다.

원인: 칼럼 높이가 **내용에 따라** 정해지고 그 합이 뷰포트를 넘으면 바깥이 늘어난다.
사이트도 `.list-scroll-area`에 `max-height` calc를 걸어 두지만 값이 뷰포트와 안 맞는다
(실측 `max-height:1003.64px`인데 바깥은 여전히 370px 넘침).

고침: 높이 사슬을 **위에서** 고정한다. 보드 높이 = 바깥 표시영역 바닥 − 보드 상단으로 못박고,
넘치는 건 전부 각 칼럼의 `.list-scroll-area` 안으로 밀어 넣는다.
**칼럼 안 스크롤은 그대로 둔다** (사용자 결정: 컴포넌트 내 스크롤은 괜찮다. 없앨 건 페이지 스크롤뿐).

### 확인된 DOM 구조 (리뉴얼 후)

- `.scheduler` > `div.scheduler-container`(**float:left**) × 3 > `.scheduler-column` × 1~3.
  - 컨테이너 안에서도 칼럼은 float다. `column2`(2칸 폭)가 두 개면 폭이 모자라 **세로로 접힌다**
    — AI 마스터 자소서 칼럼이 자기소개서 작성 지원 칼럼 **위에** 쌓이는 게 이 때문이다.
  - `column1`(1칸 폭) 세 개는 한 컨테이너 안에 가로로 늘어선다.
- 칼럼 자식은 `.title` + `.list-scroll-area` 둘뿐 → 칼럼을 flex column으로 바꿔도 안전하다.
- 바깥 스크롤러는 window가 아니라 `.resume-list-tmpl-container`다 (`document`는 안 늘어남).

### 현재 배치 규칙 — 사용자가 선택한 A안 (2026-09-07)

- `list-design.css`는 manifest의 content CSS이며 `html.jsl-fit`에서 목록에 적용한다.
  보드는 다섯 열의 grid로 배치하고 기존 컨테이너·카드·헤더·dropzone 노드를 보존한다.
- **제출 완료 패널은 내용만큼만 쓰고 남는 자리를 작성 중에 넘긴다 (2026-09-10).**
  높이를 `min(520px, 보드 48%)`로 못박아 뒀더니 카드 수와 무관하게 그만큼을 가져가,
  제출이 적을 때 그 자리가 통째로 놀았다. `flex: 0 1 auto` + `height: auto` +
  같은 값을 `max-height`로 돌려 상한만 남긴다. 실측(보드 ~1010px) —
  제출 4장일 때 작성 중 표시 368px → **657px**(잘림 265px → 0), 제출 완료 406px → 117px.
  제출 14장일 때도 작성 중이 368 → 402px.
  **카드가 많다고 자리를 더 주지는 않는다** — 제출 완료는 낸 것들의 더미이고 다시 들여다보지
  않는다는 게 사용자 결정이라, 상한 48%는 그대로 둔다.
- 왼쪽 작성 중/제출 완료는 위아래 두 패널이다. 각각 `list-scroll-area`를 추가해 독립 스크롤하며,
  기존 제목과 건수는 sticky로 유지한다. 원래 공통 스크롤러는 배치와 렌더러의 기준 노드로 남는다.
- 보드 너비 1440px 미만은 왼쪽 한 열로 전환하고 시즌과 작성 중의 상세 날짜를 감춘다.
  제목·D-day·작성량은 유지하며 카드 툴팁에 제목과 상세 날짜를 넣는다. 카드 클릭 동작은 원래대로다.
- 보드 너비 1000px 미만은 오른쪽 네 전형을 2×2로 재배치하고 왼쪽 두 패널은 함께 유지한다.
  평소 크기와 작은 창 모두 가로 스크롤 없이 전형을 표시한다. 카드 제목은 임의로 분리하거나 자르지 않는다.
- AI가 비면 만들기 버튼이 있는 제목줄만 남긴다. 내용이 있으면 최대 120px/보드 높이 20%로 제한한다.
  준비 중 공고는 최대 80px/10%, 미제출은 최대 88px/12% 안에서 스크롤해 주요 두 패널 공간을 확보한다.
- 검색·필터·정렬·보기 모드의 원래 노드/이벤트를 유지하면서 상단 여백과 정렬을 정돈한다.
- 보드 높이는 바깥 표시영역 바닥에서 보드 상단을 뺀 값이며, 초과 높이가 있으면 한 번 보정한다.
  `min-height:0` 사슬로 내용이 부모를 밀어내지 않게 한다. 변수와 활성 클래스는 root에 보관한다.
- 사용할 보드 높이가 380px 미만이면 높이 고정을 해제한다. 구조는 최대 6초 기다린 후 찾지 못하면
  경고 1회와 함께 비활성화한다. SPA 이탈 시 추가 클래스/속성/보조 노드를 정리한다.

### 사이트 토큰에 맞춘 디자인 레이어 (2026-09-09)

배치는 A안 그대로 두고 값만 사이트 실측치로 바꿨다. 채용공고 캘린더에서 추출한 값이 기준이다 —
경계선 `#eee`/`#ddd`, 본문 13px `#333`, radius **4px**, 그림자 `0 2px 5px rgba(0,0,0,.08)`,
상태 tint `#fff6f0` + 테두리 `#fed2ba`, 브랜드 주황 `#ff6813`, 보조 네이비 `#3f4b5e`.
사이트가 이미 Pretendard를 쓰므로 폰트는 상속만 받고 지정하지 않는다.

- ~~**열은 상자가 아니라 격자의 칸이다.**~~ **2026-09-10 뒤집힘 → 아래 "전형 열을 닫힌 카드로" 절 참조.**
  열 머리는 흰 바탕 44px에 상단 3px 색 띠를 두르며, 다섯 열의 머리가 같은 선에서 시작한다 — 이건 유지.
- **카드 형태는 어디서나 같다.** radius 4px, 테두리 `#eee`, hover에 사이트 그림자.
  상태는 좌측 3px 띠 색으로만 구분한다 — 마감 임박 주황 / 제출 완료 네이비 / 합격 초록 / 불합격 회색.
  합격과 불합격은 배지 크기·간격이 같으며 한쪽을 행으로 강등하지 않는다.
- **단계 색은 채도만 올린다.** 서류 `#a89c93` → 1차 `#d9955f` → 2차 `#f5802f` → 최종 `#ff6813`.
  색상은 브랜드 주황 하나이고 최종에서 원색에 도달한다. 팔레트를 늘리지 않는다.
  결과적으로 원색 주황은 화면에서 작성 중과 최종 두 곳에만 선다.
- **작성 중·제출 완료는 각각 테두리 있는 패널**로 12px 띄운다. 카드가 패널 바닥에서 잘려도
  테두리가 받아주므로 스크롤 중인 상태로 읽힌다. 패널의 위 테두리는 3px 색 띠가 대신하므로 두지 않는다.
  제출 완료는 지면을 `#eef0f4`로 내려 위 구역과 형태·지면·거리 세 가지가 동시에 달라진다.
  미제출은 카드 영역을 차지하지 않고 같은 문법의 얇은 캡슐로 눕힌다.
- 검색은 테두리 대신 `#f6f7f8` 면으로 눕히고 포커스 때만 흰 바탕 + 주황 링을 준다.
  정렬은 흩어진 텍스트를 회색 트랙 위 흰 알약(세그먼트 컨트롤)으로 묶는다. 원본 노드와 이벤트는 유지한다.
- 마감 임박은 사이트가 관심 공고에 쓰는 처리를 그대로 쓴다 — 카드 전체 `#fff6f0` + `#fed2ba`,
  D-day 칩은 주황 채움으로 반전. 사이트가 `.end-time.soon`을 붙인 카드에만 적용한다.
- 통계바는 자체 `<style>`로 따뜻한 회갈 팔레트를 쓰므로 보드 토큰으로 되돌린다.
  분수·워터마크의 명도를 올려 읽히게 한다.
- 전형 열의 빈 안내는 점선 대신 실선 + `#fafafa` 채움이다. 사이트에 점선 테두리가 없다.

미반영 — 여정 트랙(반원 게이지·화살촉 커넥터), 툴바 재배치, 빈 열 선 일러스트는 주입 노드가
필요해 CSS만으로 되지 않는다. 여정 트랙은 이후 list-stats로 구현됐다. "아래에 더 있어요" 힌트는
아래 절 참조.

### 패널 아래 스크롤 힌트 (list-layout) — 2026-09-09

작성 중 / 제출 완료 패널은 안쪽에서 스크롤되는데, 바닥에 카드가 잘려 있어도 표시가 없어
"이게 다인가?" 싶다. `list-layout`이 패널을 이미 관리하므로 힌트도 여기서 붙인다.

- 대상: `.jsl-pane[data-jsl-category="0"]`, `[data-jsl-category="1"]`. 미제출(10)은 제외.
- 각 패널 **오른쪽 아래 구석**에 `button.jsl-morehint` "N개 더" 알약을 띄운다. 흰 바탕 +
  주황 글씨/테두리(제출 완료는 네이비), 아래꺾쇠 아이콘. 누르면 그 `> .dropzone`을
  한 화면(≈85%) 아래로 부드럽게 스크롤한다. 사이트 카드/DOM/이벤트는 건드리지 않는다.
- **바닥 가운데 → 오른쪽 아래로 옮겼다 (2026-09-10).** 가운데는 두 카드 열의 경계라
  양쪽 카드를 걸쳐 물었고, 특히 오른쪽 카드의 회사 이름 앞글자를 먹어 글자가 깨진 것처럼
  보였다 (실측: 이름 23px ≈ 2글자 침범. "포스코 AI - 사무DX"가 "코 AI - 사무DX"로 보임).
  회사 이름은 카드 왼쪽부터 쓰이므로 오른쪽 아래 구석은 대개 비어 있다.
  실측 근거: 작성 중 16장 + 제출 완료 13장 모두 글자가 카드 오른쪽 끝에서 최소 164px 떨어져
  끝나고(알약이 차지하는 폭은 82px), 말줄임표가 걸린 카드도 0장이다. 최악의 경우에 먹히는 건
  이름의 꼬리라 어느 회사인지는 앞부분으로 읽힌다. 알약이 덮는 건 진행률 분수 자리인데,
  같은 값을 카드 바닥의 2px 막대가 이미 그림으로 보여준다.
- **반려된 안** — ① 알약 밑을 흰색 그라데이션으로 흐리기: 눈으로 차이가 안 나 문제 해결이 안 됨.
  ② 바닥 전체를 덮는 한 줄 띠: 겹침은 사라지나 카드 자리를 42px 뺏고 알약보다 무겁다 (사용자 반려).
- **dropzone에 아래 여백을 주지 않는다.** `scrollHeight`가 늘어나면 "맨 아래 도달" 판정과
  맞물려 알약이 떴다 사라졌다 하는 되먹임이 생긴다. 알약은 겹쳐 뜨기만 한다.
- 표시 조건: dropzone에 세로 스크롤 여유가 8px 넘게 남고, fold 아래로 완전히 넘어간
  `li.resume-node`가 1개 이상일 때만. 맨 아래에 닿거나 안 잘렸으면 `hidden`.
- 드래그 중에는 감춘다. dropzone `scroll`에 rAF 스로틀로 갱신하고, `apply()` 끝에서도 한 번 부른다.
- N이 바뀔 때만 innerHTML을 다시 쓴다(`data-jsl-n` 가드). `.jsl-morehint`의 childList/attr 변경은
  레이아웃 옵저버가 무시한다(`.jsl-tile`·`.jsl-seg`와 같은 처리). SPA 이탈·release 시 노드를 제거한다.

### 보드 좌우 가장자리의 회색 띠 (2026-09-09)

증상: 보드 맨 오른쪽 가장자리에 회색 세로 띠가 보인다. 왼쪽도 정도는 덜하지만 같다.

실측(2327×1194, 확장 ON/OFF 비교) — **확장이 만든 여백이 아니다.**

| 요소 | 좌우 | 배경 |
| --- | --- | --- |
| `.resume-list-tmpl-container` | L0 → R2327, **`padding: 0 32px`** (사이트 값) | 투명 |
| `.main-tab` (그 뒤) | L0 → R2327 | **`#f5f5f5`** ← 보이는 회색 |
| `.scheduler-resume-list-ctrl` | **L32 → R2295** | 사이트 기본 `transparent`, 우리가 `#fff` |
| `.resume-search-section` (툴바) | **L0 → R2327** (사이트 `margin: 0 -32px` 상쇄) | 우리가 `#fff` |
| `.scheduler` (보드) | L32 → R2295 | 투명 |

`jsl-fit`을 떼고 재측정해도 32px padding과 툴바의 `-32px` 풀블리드 상쇄는 그대로 있다.
회색 자체는 사이트 것이다. 확장이 만든 건 여백이 아니라 **단차**다 —

- 우리가 흰색으로 칠한 툴바는 사이트의 상쇄 덕에 화면 끝까지 가는데, 그 아래 보드는 32px
  안쪽에서 멈춘다. 툴바 아래(y≈147)부터 바닥까지 좌우 32px에 **ㄱ자 회색 단차**가 생긴다.
- A안 이전엔 열이 회색 지면 위에 뜬 상자들이었지만 지금 보드는 이음매 없는 흰 판이다.
  그래서 남은 회색이 "페이지 여백"이 아니라 "끝나다 만 가장자리"로 읽힌다.
- 오른쪽이 더 심한 이유: 왼쪽 끝은 `작성 중` 패널의 1px 테두리 + radius가 흰색을 끊어주지만,
  마지막 전형 열은 `border-left`만 있는 맨 격자 칸이라 **오른쪽을 끊는 선이 없다.**

**결정 — 시안 2안 "흰 여백" (사용자 선택).** 열 위치는 그대로 두고 그 padding 자리를 보드
지면과 같은 색으로 덮는다. `html.jsl-fit`에서 `.resume-list-tmpl-container`를 칠한다.

지면 색을 `#fafafa`로 내려 전형 카드를 띄워 본 적이 있으나 **되돌렸다** (2026-09-10).
좌우 단차는 없어졌지만 툴바는 흰색이고 그 아래만 회색이라 **가로로 색이 갈리는 이질감**이
생겼다 (사용자 지적). 지면은 흰색으로 두고 카드는 색이 아니라 그림자로 띄운다 — 아래
"전형 열을 닫힌 카드로" 절의 `--jsl-lift` 참조.

- 32px은 **좌우 대칭 여백**으로 남는다. 카드가 화면 끝에 붙지 않아 숨 쉴 틈이 생기고,
  ㄱ자 단차는 사라진다. 격자·열 폭·카드 폭을 하나도 건드리지 않는다.
- 풀블리드(카드 폭 +64px), 8px 대칭 거터, 보드를 카드로 감싸기, 우측 정렬은 검토 후 반려.
  회색을 없애는 게 목적이지 배치를 다시 짜는 게 목적이 아니다.
- 같은 규칙이 `ctrl` 아래 20px(컨테이너 아래 여백)의 회색도 함께 덮는다.
- `jsl-compact`·`jsl-narrow`에 이 배경을 뒤집는 규칙이 없어 세 상태 모두 흰색이다.
  채팅 열림에서는 아래 절의 `padding-right: 0`이 겹쳐 오른쪽만 보드가 끝까지 가고
  왼쪽 32px 흰 여백은 남는다(비대칭이지만 아래 절의 결정을 따른다).

### 전형 열을 닫힌 카드로 (2026-09-10)

사용자가 확장 적용 전 원본 화면과 비교해 "전형이 구별되어 보이고 아래 여백이 있는 게 낫다"고 지적했다.
원본이 편했던 이유는 구분 장치가 많아서가 아니다 — 오히려 지금 쪽이 번호 배지·3px 단계 띠·세로
헤어라인으로 더 많이 나눠 놨다. 원본은 **전형이 끝이 있는 카드**였고 지금은 **끝없는 격자 칸**이다.
카드가 한두 장뿐인 1차 전형은 그 아래로 화면 바닥까지 흰 공백이 이어져, "비어 있다"가 아니라
"덜 그려졌다"로 읽힌다. 같은 빈 공간이라도 원본에서는 카드 **바깥**이라 자연스러웠다.

**결정 — 시안 1안 (사용자 선택).** 열마다 테두리를 닫아 카드로 만들고 높이는 그대로 둔다.

- `.scheduler`는 `gap: 12px`, 자기 `border-top`은 뗀다. 전형 열은 `1px solid #eee` +
  `radius 7px` + 흰 바탕. 빈 공간이 카드 안에 갇혀 "여기까지가 이 전형"이 된다.
- **경계는 그림자로 만든다** — `--jsl-lift: 0 1px 2px rgba(0,0,0,.05), 0 4px 14px rgba(0,0,0,.08)`.
  흰 지면에 흰 카드 + `#eee` 테두리로 처음 냈더니 경계가 안 읽혔다. 지면을 `#fafafa`로
  내려도 봤으나 툴바와 가로로 색이 갈려 되돌렸다. 남은 방법이 **높이**다 — 색을 더하지 않고
  카드를 띄운다. 사이트 그림자(`--jsl-sh`)는 작은 카드용이라 화면 높이만 한 열에는 약해서
  같은 계열의 2단 그림자를 따로 뒀다. `.jsl-pane`(작성 중·제출 완료)도 같은 값으로 띄워
  다섯 열이 같은 높이에 뜬다.
- 보드의 `overflow: hidden` 때문에 **바깥 테두리 쪽 그림자는 잘린다**(맨 왼쪽·오른쪽·아래).
  열 사이 12px 고랑의 그림자는 안 잘리고, 구분은 거기서 생기므로 실사용에 지장 없다.
- **열에 `overflow: hidden`을 걸지 않는다.** 드래그 중인 카드가 열 경계에서 잘린다.
  3px 단계 띠는 자기 `border-radius`로 카드 모서리를 따라가게 한다.
- 보드 높이를 `calc(var(--jsl-board-h) - 12px)`로 줄인다. 그대로 쓰면 카드의 아래 테두리와
  둥근 모서리가 화면 끝에 붙어 잘린다. `.jsl-work-group`의 아래 padding은 0으로 해 왼쪽 패널
  바닥선을 전형 카드 바닥선과 맞춘다.
- `.list-scroll-area`의 아래 padding을 14px → 22px로 늘려 마지막 카드 아래에 숨 쉴 틈을 준다.
  전형 열 UL의 `padding-bottom: 26px`(드롭 자리 확보)은 그대로 둔다.
- 색은 하나도 추가하지 않았다. 테두리·간격·아래 여백만 바뀐다.

**반려된 안** — ② 열이 내용 높이에서 멈추기(원본에 가장 가깝지만, 짧아진 열의 빈 아래쪽이
사라져 드롭 자리가 좁아진다. 드래그 중에만 열을 다시 펴는 처리가 추가로 필요),
③ 전형 전체를 큰 카드 하나로, ④ 아래 여백과 바닥선만, ⑤ 합격/불합격 묶음을 각각 상자로.

### 미제출로 드래그해 넣기 (2026-09-10)

미제출 칸으로는 카드를 끌어다 놓을 수 없었다. 실측 — 카드가 0장이면 패널이 제목줄만 남는
41px 캡슐이라 `> .dropzone` 0px, `ul.itemlist[category_key="10"]` 0px다. 원본 DnD는 **ul의
사각형**으로 드롭을 판정하므로(위 "사이트 렌더러·드래그 호환 처리" 참조) 판정 사각형이 아예
없었다. 사이트가 미제출을 막은 게 아니다 — 우리 배치가 받을 자리를 0으로 만든 것이다.

평소에 자리를 뺏지 않으면서 받을 수는 있어야 하므로 **드래그하는 동안만 연다.**

- `list-layout`의 `prepareDrag`가 `html.jsl-dragging`을 켜고 `restoreDrag`/`release`가 끈다.
  `mousedown`의 **캡처 단계**라 사이트가 좌표를 재기 전에 배치가 끝난다 — 드래그 자체는
  영향받지 않는다.
- CSS는 `html.jsl-fit.jsl-dragging`에서 미제출 패널의 `max-height`를 `min(112px, 보드 14%)`로
  올리고, 비어 있을 때 `> .dropzone`에 padding을, ul에 `min-height: 46px`을 준다.
  `min-height`는 `max-height`(`--jsl-hit-h`)를 이기므로 `updateDragBounds`가 잘라도 남는다.
- 패널이 **위로** 자란다(바닥 고정). 늘어난 만큼은 위 두 패널이 내준다.

**실측 검증 (드롭은 커밋하지 않고 mouseup 없이 새로고침으로 중단).**

| | 미제출 ul 높이 | helper | placeholder |
| --- | --- | --- | --- |
| 고치기 전 | **0px** | 생김 | **안 생김** |
| 고친 뒤 | **46px** | 생김 | **미제출(category 10) 안에 들어감** |

### 정렬 세그먼트 트랙이 화면 끝까지 늘어나던 것 (2026-09-10)

`.resume-order-by`는 흩어진 정렬 텍스트를 회색 트랙 위 흰 알약으로 묶는 세그먼트 컨트롤이다.
`display: inline-flex`로 내용만큼만 줄이려 했으나 실측 폭이 **1,682px**이었다 (항목은 263px).
부모 `.sort-resume-list`가 `.resume-search-body` 그리드의 둘째 칸(1,800px대)이고, 블록 레벨
flex 컨테이너가 그 칸을 꽉 채운다. 결과적으로 회색 트랙이 항목이 끝난 뒤에도 화면 오른쪽 끝까지
이어져, 가로의 3분의 2가 빈 회색 막대로 남았다 — 묶을 게 없는 곳까지 묶어 놓아 로딩이 덜 된 것처럼 보였다.

`width: fit-content`로 묶는 범위를 항목까지 자른다 (1,682px → 269px).

**남는 오른쪽은 비워 둔다 (사용자 선택 A안).** 바로 윗줄 오른쪽에 이미 `삭제한 문서`와
`스케쥴러 보기`가 앉아 있어 아랫줄까지 채우면 오른쪽 위가 빽빽해진다. 정렬을 오른쪽 끝으로
미는 안, 검색창을 넓히는 안은 검토 후 반려. 나중에 거르개 같은 기능이 생기면 그 자리를 쓴다.

### 채팅 패널을 열었을 때의 오른쪽 여백 (2026-09-09)

채팅을 열면 보드와 채팅 패널 사이에 32px 흰 띠가 생긴다. `.resume-list-tmpl-container`의
좌우 여백인데, 왼쪽은 페이지 여백으로 읽히지만 오른쪽은 두 화면 사이의 틈으로 보인다.
채팅 패널이 자기 왼쪽 1px 경계선을 갖고 있으므로 `.main-tab.open-chat`에서 오른쪽 여백만 걷는다.

`.resume-search-section`은 `margin: 0 -32px` + `padding: 0 32px`로 그 여백을 상쇄해
컨테이너 폭을 꽉 채우고 있다. 오른쪽 여백만 걷고 이 상쇄를 그대로 두면 32px이 넘쳐
가로 스크롤바가 생기고, 그 높이 때문에 세로 스크롤바까지 따라 뜬다. 둘을 같이 푼다.

### 사이트 렌더러·드래그 호환 처리

- 원본 `I()`는 왼쪽 모든 목록에 하나의 scrollTop과 max-height를 적용한다. 독립 스크롤과
  한 열 배치를 위해 왼쪽 원본 렌더 범위를 `999999px`로 확장하고 CSS grid가 카드 위치를 계산한다.
  원본 카드 생성·캐시·순서를 사용하지만 왼쪽 가상화의 DOM 절감 효과는 포기한다. 수천 개 이상의
  카드 성능을 보장하지 않는다. 렌더 높이는 `parseInt`가 읽을 수 있는 유한한 십진 px이어야 한다.
  `none`은 NaN, `10000000px`의 계산값 `1e+07px`은 1이 되므로 둘 다 사용할 수 없다.
- 드래그 동안 왼쪽의 `column2`를 `column1`로 바꿔 사이트가 실제 카드 경계와 DOM 순서를 쓰는
  일반 목록 이동 경로를 실행하게 한다. 별도 정렬 API나 데이터 변경 경로를 추가하지 않는다.
- 이 동안 원본 왼쪽 재렌더가 placeholder를 지우지 않게 하고, 숨긴 기준 노드는 `I()`의 필수
  스크롤 참조를 유지한다. pointer up capture에서 원래 클래스를 복원한 후 원본 q()/I()가 실행된다.
- 원본 DnD는 overflow에 가려진 UL까지 드롭 대상으로 판정한다. 드래그 중 UL의 최대 높이를
  해당 패널 바닥에 맞추고 스크롤/포인터 이동 때 갱신한다. 실제 카드의 overflow는 유지해 끝까지 스크롤한다.
  드래그 중 scroll anchoring을 끄고, 종료 시 임시 크기와 클래스를 복원한다.
- 아래 기존 패치 기록은 A안 이전의 관찰과 검증이다. 현재 배치 규칙과 구분한다.

### 카드가 사라진 원인과 수정 (2026-09-07)

실페이지에서 작성 중 15장·제출 완료 12장은 `ul`의 높이만 남고 **`li`가 0개**였다.
다른 칼럼의 279장은 존재했다. 진행률 타일도 0개여서, 카드가 타일에 가려졌다는
가설과 달랐다. 당시 두 목록의 높이는 각각 696px·448px, 스크롤러의 계산된
`max-height`는 `none`이었다.

사이트 배포 스크립트의 2열 가상 목록 렌더러는 `.list-scroll-area`의
`max-height`를 `parseInt`로 읽어 표시 범위의 하한·상한을 구한다.
`none`이면 상한이 `NaN`이 되어 모든 카드의 범위 판정이 실패한다.
이때 렌더러는 전체 목록 높이를 유지하면서 기존 `li`를 제거한다.
따라서 렌더 이후 CSS를 한 번 주입했을 때 보여도, 다음 스크롤·재렌더부터 비게 될 수 있다.
높이 사슬·sticky·진행률 타일을 제거하는 대신 숫자 px 상한을 유지하도록 수정했다.

- 근거: 실페이지 DOM 및 [당시 사이트 배포 스크립트](https://d2bovrvbszerbl.cloudfront.net/assets/application_web_main-c19f3aa9323e12d0508ef4641974d4f9bc247e45af46c50aed3a554c06579353.js)의 가상 목록 함수 `I()`.
- 가상 목록이므로 작성 중·제출 완료의 모든 `li`가 항상 DOM에 있을 필요는 없다.
  검증할 때는 스크롤하면서 각 구간의 카드가 실제로 나타나는지 확인한다.
- 로컬 격리 검사 9개 통과: 보드 높이, 사이트의 `none`/px 범위 판정, 초과 높이 보정,
  짧은 화면 해제·복구, 화면 이동 시 해제, 초기 렌더 대기, 구조 누락 경고 1회,
  SPA 진입 시 구조 누락 감지, 레이아웃 모듈의 네트워크·액션 호출 없음.
- 별도 Chrome 재현 페이지의 비교 검사 11개 통과: 실제 `list-layout`·`list-progress`와
  사이트의 표시 범위 판정을 재현한 테스트 목록을 사용했다. `none`에서 카드 0개·목록 높이
  648px을 재현했고, 다른 CSS 규칙 6개를 각각 제외해도 카드가 없었다.
  숫자 상한 복구 후 카드 15개·타일 15개, 상태 20회 적용 후 유지, 바깥 세로 스크롤 0px을
  확인했다. 이는 실제 Angular 보드 전체를 실행한 검사는 아니다.

### 카드 소실 패치의 실페이지 재검증 — 반응형 수정 전 (2026-09-07)

사용자가 확장을 새로고침한 뒤 Chrome에서 직접 확인했다. 기본 뷰포트는 2560×1261이었다.

| 확인 항목 | 결과 |
|---|---|
| 바깥 `.resume-list-tmpl-container` | `scrollHeight - clientHeight = 0px` |
| 작성 중 / 제출 완료 / 서류 불합격 | 15장 / 12장 / 219장 DOM 및 화면 표시 확인 |
| 작성 중 진행률 타일 | 초기 및 기본 크기 복원 후 15개 |
| 작성지원 `.list-scroll-area` | 표시 840px / 내용 1,354px, `scrollTop` 0 → 514px |
| 서류전형 `.list-scroll-area` | 표시 879px / 내용 13,117px, 마지막 카드까지 스크롤 확인 |
| 1차 전형 `.list-scroll-area` | 마지막 카드까지 내부 스크롤 확인 |
| 통계바 / 자비스 경고·오류 | 통계바 유지 / 마지막 조회에서 경고·오류 없음 |
| 1920×1080 / 1366×768 | 두 크기 모두 바깥 세로 스크롤 0px, 카드 소실 재발 없음 |
| 검증 후 복원 | 뷰포트 강제 설정 해제·기본 크기에서 새로고침 후 위 카드 수와 타일 유지 |

작성지원 칼럼을 끝까지 스크롤하면 가상 목록이 작성 중 `li`를 9개만 유지하고
제출 완료 12장을 렌더했다. 이때 작성 중 타일도 9개로 일치하며, 이전의 카드 전체 소실과 다르다.
내용이 영역 안에 들어가는 최종 전형이나 비어 있는 AI 칼럼에 내부 스크롤을 강제하지 않는다.

**이 시점의 리사이즈는 부분 통과였다.** 1366×768에서 2열 카드 폭은 약 130px로 줄고 카드 높이는
최대 약 197px로 늘었지만, 다음 행의 `top`은 87px로 남아 카드가 겹쳤다.
같은 크기에서 새로고침해도 다음 행 `top` 124px·143px에 대해 실제 카드 높이
약 197px·179px여서 겹침이 남았다. 이 관찰을 바탕으로 아래 반응형 수정을 진행했다.

실제 드래그·드롭과 드래그 중 자동 스크롤은 **미검증**이다. 사이트의 드래그 종료 경로
`stopHandler → j() → q()`가 `/resume/scheduler_reorder`로 POST를 보내므로,
데이터 쓰기 금지 조건에 따라 실행하지 않았다. 관심공고 섹션도 현재 화면에 없어
해당 섹션과의 동시 표시를 검증하지 못했다. 자동 정렬 교정도 별도로 실행하지 않았다.

### 반응형 폭과 진행률 타일의 높이 측정 수정 (2026-09-07)

겹침의 직접 원인은 사이트가 `N()`에서 타일 없는 카드의 높이를 캐시한 뒤,
`list-progress.reserveSpace()`가 왼쪽 여백 46px·아래 여백 5px을 더한 것이었다.
좁은 카드에서 제목이 더 많이 줄바꿈되어 실제 높이가 커져도 `I()`는 이전 높이로
`top`을 배치했다. 새로고침도 같은 순서로 실행되므로 해결되지 않았다.

`list-progress`가 생성·측정 단계부터 같은 여백을 적용하도록 바꾸고,
`list-layout`에는 비율 조절과 최소 카드 폭을 추가했다. 카드 높이를 고정하거나
내용을 잘라 숨기지 않는다. 칼럼 안 세로 스크롤러와 사이트의 2열·드래그 구조를 유지한다.

로컬 Chrome 재현 페이지에 당시 사이트 CSS와 가상 목록 함수 `N()`·`I()`,
`scheduleDnD()`를 그대로 사용하고 가상 카드 및 서버 저장 없는 드롭 콜백을 연결했다.
현재 작업의 `list-layout`·`list-progress`·`list-stats`를 함께 실행했다.

- 1920×1080: 카드 겹침 0건, 타일 누락 0개, 바깥 세로 스크롤 0px, 보드 가로 초과 0px.
- 1366×768: 카드 겹침 0건, 타일 누락 0개, 바깥 세로 스크롤 0px,
  보드 내부 가로 초과 509px, 최소 카드 폭 약 211px.
- 작성지원 끝까지 스크롤한 뒤 제출 완료 12장을 렌더했고, 겹침·바깥 스크롤은 없었다.
- 로컬 드래그: 작성 중에서 서류 불합격 칼럼으로 드롭되는 것, 드래그 중 자동 스크롤
  약 22px 진행, 드래그 카드가 표시되는 것을 확인했다. 실제 서버 저장 검증은 아니다.
- 수정된 확장을 다시 새로고침한 실페이지 검증은 별도로 필요하다.

### 이전 콘솔 단회 주입 실측 (2327×1194, 자소서 306장)

아래는 렌더가 끝난 페이지에 주입했던 당시 측정값이다. 수정된 확장의 회귀 검증과 구분한다.

| | 적용 전 | 적용 후 |
|---|---|---|
| 바깥(페이지) 스크롤 | 370px | **0px** |
| 보드 하단 | 화면 밖 | 화면 바닥에 정확히 맞음 |
| 자기소개서 작성 지원 칼럼 | 1,073px(잘림) | 842px, 내부 스크롤 |
| 서류전형 칼럼 | 1,073px(잘림) | 881px, 내부 스크롤 |
| 보드 가로 스크롤 | — | 0px (다 들어감) |

### A안 검증 상태 (2026-09-07)

로컬 Chrome에 원본 CSS, N/I 렌더러, scheduleDnD와 q() 순서 갱신을 사용했다.
카드는 가상 데이터이며 원본 POST는 기록용 mock으로 대체하고 CSP로 네트워크를 차단했다.
실제 계정의 이동·정렬 검사는 실행하지 않았다.

- 1920×1080 및 1366×768에서 기본 45개 카드 전부 표시, 카드 겹침/제목 영역 초과 0건,
  바깥 세로 초과/보드 가로 초과 0px.
- 900×768에서도 오른쪽 네 전형이 2×2로 화면 안에 들어오며 카드 겹침/보드 가로 초과가 없었다.
- 각 크기에서 원본 카드 클릭 콜백, 같은 목록 순서 변경, 작성 중→제출 완료,
  왼쪽→서류 불합격, 서류 불합격→작성 중, 빈 최종 전형 및 접힌 작성 중 제목으로 이동 확인.
  mock 요청의 대상 category/id, 결과 배열의 순서, 드래그 카드 표시와 자동 스크롤을 확인했다.
- 작성 중 180개·제출 완료 120개·AI 5개·미제출 3개와 준비 중 공고 대체 UI를 함께 표시했다.
  두 주요 목록의 독립 스크롤과 sticky 제목, 구조 재생성 및 이탈 시 클래스/속성/보조 노드 정리 확인.
- 작은 창에서 작성 중을 약 14,653px 스크롤하는 동안 제출 완료는 0 유지;
  제출 완료를 약 7,608px 스크롤한 뒤에도 작성 중의 위치 유지.
- 기본 크기의 재진입 1.4초 시점에는 원본 N()의 비동기 렌더 도중 왼쪽 카드가 0개인 표본이 있었다.
  3초 완료 시점과 후속 측정에서 180/120/5/3개가 모두 복구됨을 확인했다. 순간 깜빡임이 없다는 검증은 아니다.
- Node 구문 검사와 manifest의 18개 content 자산 존재 검사, diff 공백 검사를 통과했다.
- 이 로컬 검사는 전체 Angular 앱, 서버 저장/응답, 실제 검색·정렬 API, 실제 관심공고 데이터 흐름을 포함하지 않는다.

#### 실제 사이트 적용 확인

사용자가 확장을 새로고침한 뒤 실제 목록 페이지도 새로고침했다. 새 `.jsl-pane` 3개와
작은 진척 표시가 적용됐고, 2026 하반기의 카드 45개(작성 중 15/제출 완료 12)를 확인했다.

- 2560×1261, 1366×768, 900×768에서 카드 겹침/제목 영역 초과/진척 표시의 카드 밖 초과 0건,
  바깥 가로·세로 초과와 보드 가로 초과 0px. 모든 전형 제목이 화면 안에 들어왔다.
- 1366px에서는 왼쪽 한 열과 보조 정보 간소화, 900px에서는 오른쪽 네 전형의 2×2 배치를 확인했다.
- 기본 크기에서 작성 중을 끝까지 204px 스크롤해도 제출 완료는 0을 유지했다.
  제출 완료를 끝까지 40px 스크롤한 뒤에도 작성 중은 204px을 유지했다.
- 1366px에서 작성 중은 1,067px, 제출 완료는 673px까지 각각 독립적으로 스크롤했다.
  두 제목은 고정됐고 각 목록의 마지막 카드가 패널 안에 온전히 보였다.
- 실제 검색 버튼으로 회사명 검색 결과를 확인하고 검색 해제로 목록 복귀를 확인했다.
  원본 카드의 마우스 입력으로 상세 모달과 해당 자소서 쓰기 링크가 열리는 것을 확인했다.
- 확인 후 상세 모달을 닫고 검색을 비웠으며, 화면 크기를 기본값으로 복원하고 두 목록을 맨 위로 돌렸다.
- 실제 지원 데이터의 이동·정렬·저장은 실행하지 않았다. 해당 변경 동작의 근거는 위 로컬 mock 검사다.
  실제 관심공고 데이터 흐름 및 수천 건 규모의 성능은 이번 확인에 포함하지 않는다.

실제 측정 기록은 디자인 산출물 폴더의 `live-verification.json`, 화면은
`live-desktop.png`, `live-small-a.png`, `live-narrow.png`에 저장했다.

### 목록 테마 — 주황색 사용 절제, 2026-09-07

눈이 피로하고 테마색이 반복된다는 피드백에 따라 주황색 사용 범위를 줄였다.
- 작성 중 제목은 흰 바탕과 왼쪽 3px 주황 표시로 변경한다.
- 다른 전형의 상단 주황선과 제출 완료의 주황 표시를 제거한다.
- 일반 마감 배지는 중성색, 사이트가 실제 임박 상태로 표시한 배지만 약한 주황색으로 구분한다.
- 정렬 선택은 주황색 채움 대신 흰 바탕의 가는 밑줄로 표시한다.
- 주황색은 작은 강조/선택/작성된 진행 막대에 집중한다. AI 만들기 버튼은 중성색으로 정리한다.
- 배경과 테두리 대비는 중간 수준으로 완화하고, 글자 가독성과 배치/크기/동작은 유지한다.

### 전형 구분과 빈 상태 — 2026-09-07

- 서류/1차/2차/최종 전형 제목에 회색 01/02/03/04 단계 표시를 추가한다.
- 기존 합격 제목과 건수는 작은 청록색 체크 배지, 불합격은 중성색 배지로 표시한다.
- 0건 구역은 제목 여백을 줄이고, 최소 56px 높이의 점선 안내 카드를 표시한다.
- 합격/불합격 각각 0건이면 그 구역 안의 점선 카드에 '아직 등록된 전형이 없습니다'를 표시한다.
  안내는 원본 UL의 가상 요소이므로 실제 카드 수·정렬에 포함되지 않는다. 접히거나 드래그 placeholder/실제 카드가 들어오면 숨긴다.
- 빈 상태는 목록 상태의 카테고리별 건수와 실제 카드 DOM을 함께 확인한다.
  드래그 placeholder가 들어오면 빈 목록의 높이를 강제하지 않는다.
- 원본 제목·건수·접기·드롭 노드는 유지하고 속성과 CSS로 꾸민다. SPA 정리 시 속성을 복원한다.
- 로컬 원본 렌더러 검사: 45개 카드, 겹침/가로 초과 없음. 빈 최종 전형 및 접힌 제목으로의 mock 드롭 성공, 카드 추가 후 빈 안내 해제 확인. 실제 지원 데이터는 변경하지 않았다.

### 상태 제목과 카드의 경계 보강 — 2026-09-08

- 작성 중은 따뜻한 회색 제목 띠와 연필 표시, 제출 완료는 푸른 회색 제목 띠와 체크 표시를 쓴다.
  작은 왼쪽 선과 제목 배경으로 상태를 구분하고 카드 본문에 색을 반복하지 않는다.
- 작성 중·제출 완료 외곽선을 보강하고 패널 사이 간격을 10px에서 16px로 늘린다.
  목록 아래에서 잘린 카드와 다음 상태 제목이 이어져 보이는 문제를 줄인다.
- 합격·불합격 제목은 각각 차분한 청록 회색·중성 회색의 전체 폭 제목 띠로 표시한다.
  두 결과 구역 사이에 가는 구분선을 둔다. 기존 접기·스크롤·카드 노드는 유지한다.
- 로컬 원본 렌더러 화면에서 제목 배경 적용, 패널 간격 약 16px, 페이지 가로 초과 0px를 확인했다.
  실제 사이트의 확장 새로고침 후 확인은 별도다.
- 제출 완료(`category_key="1"`) 카드는 마감일·보조 정보를 숨기고 제목 한 줄, 높이 48px로 통일한다.
  긴 제목은 데이터 수정 없이 CSS 말줄임표로 표시한다. 로컬 카드 12개의 높이 48px,
  두 열의 행 위치 일치, 긴 제목 말줄임, 가로 초과 0px를 확인했다.
- AI 마스터 자소서 영역과 만들기 버튼은 목록에서 숨긴다. 왼쪽 그룹의 불필요한 위 간격을 없애고
  작성 중과 01~04 제목의 최소 높이를 48px로 맞춘다. 로컬 기본 창에서 다섯 제목의 위 좌표가 일치했다.
- 01~04에는 청회색·청록 회색·보라 회색·갈색 계열의 차분한 3px 제목 띠를 각각 적용한다.
  번호 배경에도 같은 계열의 옅은 색을 사용하며 카드 본문에는 단계 색을 추가하지 않는다.

### 목록 색감 정리 — 2026-09-08

- 물먹은 듯한 색감이라는 피드백에 따라 넓은 베이지·청회색·청록 배경을 제거했다.
  보드 바탕은 중성 회색 `#f3f3f3`, 제목·카드는 흰색으로 통일하고 테두리·보조 글자도 중성 회색으로 정리한다.
- 상태 색은 작성 중·제출 완료의 작은 아이콘과 옆 띠, 01~04의 가는 띠와 번호에 집중한다.
  합격·불합격의 전체 폭 색 배경은 제거하고 작은 체크·대시 표시로 구분한다.
- 기존 구역 간격·외곽선·제목 높이·제출 완료 말줄임은 유지한다. 로컬에서 모든 상태 제목의 흰 배경,
  다섯 제목의 시작 높이 일치, 제출 완료 카드 48px, 가로 초과 0px를 확인했다.
- 후속 간격 수정: 실제 페이지에서 빈 상태 제목 33px/일반 제목 38px의 차이와 스크롤바용 오른쪽 여백을 확인했다.
  결과 제목은 38px로 통일하고 전형 본문 위에 8px 간격을 둔다. 스크롤바 공간은 양쪽에 대칭으로 확보한다.
  수정 후 로컬 네 전형에서 첫 카드/빈 안내의 시작 높이 일치, 좌우 여백 일치, 가로 초과 0px를 확인했다.
- 상태 제목이 공고 카드와 혼동된다는 후속 피드백에 따라 제목의 흰 박스·테두리를 제거했다.
  합격은 작은 초록 체크 배지, 불합격은 회색 × 배지로 구분한다. 제목의 전체 클릭 영역과 높이 38px는 유지한다.
  로컬에서 여덟 상태 제목의 배경·아이콘 구분, 동일 높이, 가로 초과 0px를 확인했다.
- 좌우 이질감을 줄이기 위해 작성 중·제출 완료의 별도 진한 외곽선과 그림자를 제거하고
  전형 칼럼과 같은 테두리를 사용한다. 카드 제목 굵기는 500, 큰 제목 아이콘은 24×22px로 통일한다.
  왼쪽 본문 여백은 얇은 오른쪽 스크롤바 공간을 고려해 보강했다. 로컬 확대율에서 좌우 칼럼의
  안쪽 여백 차이는 1px 이내였고, 가로 초과는 0px였다. 왼쪽 2열·독립 스크롤 구조는 유지한다.

- 작성 중·제출 완료 구분을 보강하기 위해 제목 줄에 각각 옅은 주황(`#fff6ef`)과 회청색(`#e6edf3`)을 적용한다.
  제출 완료의 위쪽 경계와 체크 아이콘도 회청색으로 구분한다. 본문과 카드 색, 제목 높이 48px,
  패널 간격 16px는 유지한다. 로컬 1280px 화면에서 두 제목 색과 높이, 가로 초과 없음 확인.

### 목록 반응 개선 — 2026-09-07

- 원본 dropzone의 0.2초 높이 transition과 animation을 제거한다.
  Angular의 hide-add/remove 상태에도 즉시 표시/숨김을 적용하고,
  오른쪽 전형 펼치기는 원본의 200ms height:auto 타이머를 기다리지 않도록 CSS 높이를 자동으로 둔다.
- 카테고리별 건수가 같은 주기 상태는 보드 전체 재계산을 생략한다.
  카드 추가/삭제/이동/재생성 및 제목 변경은 관련 DOM 변경으로 계속 처리한다.
- 레이아웃 관찰은 보드/검색 상단/통계/관심공고에 영향을 주는 변경만 처리한다.
  진행 막대 내부 변경과 목록 밖 모달/기타 DOM 변경으로 전체 레이아웃을 다시 검사하지 않는다.
- 진척도 관찰은 실제 카드 DOM 변경에만 상태 조회를 예약한다. 기존 동일 값 paint 생략 처리는 유지한다.
- 관련 DOM 변경 처리의 추가 80ms 지연을 제거한다. resize 지연과 연속 진척도 요청 제한은 유지한다.
- 로컬 확인: 같은 상태 20회에서 보드 DOM 변경 0건, 무관한 변경 100회에서 추가 상태 조회 0회,
  실제 진척도 값 변경은 2/4로 반영, 닫기/열기 display 전환 및 transition 0s 확인.
- 로컬 테스트는 원본 렌더러/DnD와 Angular 클래스 전환의 재현이다.
  실제 사이트 Angular 전체 타이밍/네트워크 지연의 전후 측정이나 속도 배수 개선을 뜻하지 않는다.

## 기업 채용 사이트 위 세로 복사 바 (`relay-source.js` + `relay-panel.js`) — 2026-09-11

문제: 자소서를 기업마다 다른 채용 플랫폼(ATS)에 옮길 때 문항마다 자소설 탭으로 돌아가
복사 버튼을 누르고 다시 돌아와야 한다. 5문항이면 탭 전환만 10번이다.

**ATS 입력칸에 자동으로 넣는 방식은 쓰지 않는다.** `<all_urls>` 권한이 필요해 PRIVACY.md와
부딪히고, 받아도 ATS마다 DOM이 달라 기업 수만큼 만들어야 한다. 대신 **우리 막대만 얹는다** —
사이트 DOM은 읽지도 쓰지도 않으므로 어느 사이트에서든 같은 코드가 돈다. 붙여넣기는 사용자가 `Ctrl+V`로 한다.

- **권한**: `activeTab` + `scripting`. 팝업을 연 것이 사용자 조작이므로 그 탭에만, 그 순간에만
  접근이 열린다. 설치 시 "모든 사이트를 읽고 쓸 수 있음" 경고가 뜨지 않는다.
  `host_permissions`는 `https://jasoseol.com/*` 그대로다.
- **`relay-source.js`** (자소설 ISOLATED): 편집 페이지 state에서 `{resumeId, title, dDay, qnas[
  {number, question, answer, chars}]}`를 뽑아 `chrome.storage.local`의 `jslRelay` 한 칸에 쓴다.
  state는 입력마다 오므로 400ms 모았다 쓰고, 문항/제목이 그대로면 건너뛴다.
  목록 화면·state null은 무시하되 **이전 스냅샷은 지우지 않는다** — 자소설 탭을 닫고
  채용 사이트에서 쓰는 게 본래 용도다.
- **팝업**: `이 탭에 복사 바 띄우기` 줄. `chrome.scripting.executeScript`로 `relay-panel.js`를
  활성 탭에 한 번 주입한다. `http(s)`가 아닌 탭, 스냅샷이 없는 경우는 버튼을 잠그고 이유를 적는다.
- **`relay-panel.js`** (주입되는 쪽): Shadow DOM 안에서만 그린다(사이트 CSS와 충돌 차단).
  같은 탭에 다시 주입되면 스스로 닫는다(토글).

**모양 — 시안 1안 "세로 얇은 일자" (사용자 선택).** 폭 **46px**, 오른쪽 가장자리에 세로로 붙는다.

- 번호 칩을 세로로 쌓고 **복사 버튼은 현재 문항 칩 바로 아래**에 붙는다. 지금 무엇을 복사하는지가
  눈으로 붙어 보이는 쪽을 택했다(버튼 위치가 문항 따라 움직이는 것은 감수).
- **자소서 본문은 화면에 그리지 않는다** (사용자 결정). 복사에는 쓰지만 보여주지 않는다.
  칩 `title` 속성에만 문항 제목이 들어간다.
- 복사하면 그 칩이 회색으로 죽고 자동으로 다음 문항으로 넘어간다. 칩을 직접 눌러 건너뛸 수 있다.
- 세로 드래그로 위치를 옮기고 `jslRelayPos`에 기억한다. 오른쪽 가장자리에 붙는 모양은 유지한다.
- `navigator.clipboard`가 막힌 사이트를 위해 `execCommand('copy')` 폴백을 둔다.
- 자소설 쪽에서 답변을 고치면 `storage.onChanged`로 막대도 따라 갱신된다.

반려된 안 — 가로 하단 바(입력 흐름을 가림), 세로+제목(64px), 사각 카드(214×150),
정사각 번호 격자(150×150), 원형 다이얼(132×132, 조준이 필요해 목적과 반대).

## 공통 원칙

- **답변 본문 쓰기는 `setAnswer`(붙여넣기/되돌리기), `applyGptAnswers`(검증된 일괄 입력)로만.**
  question이나 resume 메타는 수정하지 않는다. 나머지 쓰기는 사이트 자체
  버튼 클릭 위임만 (`spellCheck`, `save`).
- 사이트 구조 변경으로 스코프/셀렉터를 못 찾으면: 콘솔 경고 1회 + 기능 비활성. 예외 전파 금지.
- 외부 네트워크 전송 금지(제3자 서버로 데이터 전송 금지라는 뜻). jasoseol.com 자체 API를
  동일 출처에서 조회하는 것(JD 패널의 `employment_companies` 조회 등)은 예외 — 사용자 데이터를
  어디로 보내는 게 아니라 사이트가 이미 공개 제공하는 정보를 읽어오는 것뿐이다.
- 저장은 `chrome.storage.local`만.
- 코드는 바닐라 JS (빌드 없음, `chrome://extensions` 개발자 모드 로드). 주석과 UI 문구는 한국어.
# ChatGPT 응답 일괄 입력

- `gpt-protocol.js`: 문항 제목·질문과 연결된 코드 블록 후보 추출, 질문 표시 정규화, 실제 ID·번호·질문에 대응. 외부 GPT 출력 규약은 없고 내부 입력 패킷만 version 1을 사용한다.
- `gpt-response.js` / `.css`: 완료된 assistant 응답의 markdown과 같은 부모·폭에 적용 UI를 추가한다. 원래 본문·코드 복사 UI는 유지한다. 중첩 pre에서는 바깥 pre의 단일 code.textContent를 한 번만 읽는다.
- 전략표·검수표·무관한 코드 블록을 제외하고 일부/역순 문항과 번호가 명확한 수정안을 지원한다. 번호 없는 단일 블록을 순서로 매핑하지 않는다. 질문이 유일하게 일치하면 번호가 없어도 대응할 수 있다.
- 질문 비교는 연속 공백·줄바꿈을 공백 하나로 합치고 끝의 `(500자)` 같은 명시적인 분량 메타만 제거한다. 단어 사이 공백 전체·문장부호·문구를 지우지 않는다. 정규화 결과가 중복되거나 번호·질문이 충돌하면 선택/제외가 필요하다. 답변 원문은 정규화하지 않는다.
- `gpt-background.js`: `gpt-conversation:<대화 ID>`에 지원서/문항 메타와 revision만 저장한다. 최초 적용에서 사용자가 대상을 고르고, 변경·해제를 지원한다. 연결 변경만으로 입력하지 않는다.
- 준비/입력 단계는 일회용 토큰(90초)으로 연결한다. 발신 origin/frame, 현재 대화·응답 지문, 연결 revision, 대상 탭·문서 식별자·최신 문항·기존 답변을 재검증한다. 대화별 처리와 지원서별 쓰기 잠금으로 동시 적용을 막는다. 작업자 재시작으로 준비가 사라지면 다시 적용해야 한다.
- `gpt-connect.js`: 지원서의 내부 메시지 수신. `applyGptAnswers` 호출 후 입력 후처리를 기다려 상태를 재조회하고 문항별 원문 일치를 확인한다. MAIN 액션도 경로·문항·기존 답변을 검증한 후 대상 답변만 변경한다.
- 대상 탭 0개는 연결된 지원서 열기/지원서 목록 열기, 2개 이상은 명시적 탭 선택을 제공한다. 자동 선택 후 탭이 늘거나 탭/지원서가 바뀌면 입력을 중단한다.
- UI는 응답 DOM 교체·스트리밍·SPA 대화 이동 때 오래된 버튼/준비를 무효화한다. 일반 입력은 모달 없이 실행하고 모호한 대응만 선택 화면을 제공한다.
- 입력 결과는 모델 재조회 검증이며 서버 저장 확인이 아니다. 기존 `paste`의 저장 흐름을 재사용하거나 저장 버튼을 호출하지 않는다. 사이트 입력 훅에 따른 자동 저장 여부는 별도로 확인해야 한다.
